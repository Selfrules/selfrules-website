---
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - src/app/[locale]/about/page.tsx
  - src/app/[locale]/approach/page.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-04
  - COPY-01
  - MODB-03
  - MODB-04
---

# Plan 05-02: About Page and Approach Page

## Objective

Build the About page (career timeline, 3 principles, personal section, PageCTA) and the Approach page (5 numbered prose sections, closing, PageCTA). Both are prose-driven pages at 720px max-width using i18n content from Plan 05-01. All content is bilingual (IT + EN) and complies with Modello B positioning.

## Tasks

<task id="05-02-01">
<title>Build About page with VerticalTimeline career path</title>
<read_first>
- src/i18n/messages/en.json (about namespace — verify it exists from 05-01)
- src/i18n/messages/it.json (about namespace)
- src/components/ui/ (available components: VerticalTimeline, TimelineNode, Tag, Button, Section)
- src/components/sections/section-header.tsx (SectionHeader API)
- src/components/sections/page-cta.tsx (PageCTA API)
- src/components/layout/section.tsx (Section wrapper API)
- src/app/[locale]/layout.tsx (existing locale layout pattern)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (About Page layout contract)
- ../job-search-2026/selfrules-redesign/about-page.md (source copy for verification)
</read_first>
<actions>
Create src/app/[locale]/about/page.tsx as a Server Component:

1. **Boilerplate:** Export generateStaticParams returning [{locale:'en'},{locale:'it'}]. Await params, call setRequestLocale(locale), get translations from 'about' namespace.

2. **Headline section (Section default width 720px):**
   - h1: title size clamp(28-36px), Space Grotesk 700 (font-heading), text-primary

3. **Career Path — VerticalTimeline (Section default width 720px):**
   - Use VerticalTimeline + TimelineNode components from Phase 3
   - 4 nodes: Selfrules (2012-2018), FLOWING (2016-2020), ActiveProspect/LeadsBridge (2020-2023), QubicaAMF (2023-present)
   - Last node (QubicaAMF) is active: `isActive={true}` (filled accent square)
   - FLOWING/LeadsBridge overlap (2016-2020): `isOverlap={true}` if component supports it
   - Each node: company name (subtitle 20px, Space Grotesk 700), dates (14px mono, text-secondary), prose paragraphs (16px, Inter, line-height 1.6)
   - Bold inline metrics use next-intl rich text: t.rich('career.qubicamf.text', { bold: (chunks) => <strong className="font-bold text-accent">{chunks}</strong> })
   - 48px gap between timeline nodes

4. **"What I believe" section (64px gap from career, Section default width):**
   - Optional SectionHeader or h2 for section title
   - 3 principles, each: title (subtitle 20px, Space Grotesk 700, text-primary) + body (16px, Inter 400, line-height 1.6, text-primary)
   - 48px between principles
   - Prose sequential flow (D-02), NOT cards

5. **"Outside work" section (64px gap from principles):**
   - Claude's discretion (D-03): recommend Option B — subtle visual separation with a 1px border-default divider above
   - Body text: 16px, Inter 400, text-primary

6. **PageCTA:**
   - text: t('cta.text')
   - primaryCta: { label: t('cta.primary'), href: 'mailto:mattia@selfrules.org' }
   - 80px top margin (handled by PageCTA component)

CRITICAL: Career blocks use prose sections, NOT CaseStudyCard or TimelineBlock cards (D-01). Stories lead with the problem/result, not "I worked on" (MODB-04) — the source copy already follows this, just preserve it.
</actions>
<acceptance_criteria>
- file src/app/[locale]/about/page.tsx exists
- grep "generateStaticParams" src/app/[locale]/about/page.tsx returns a match
- grep "setRequestLocale" src/app/[locale]/about/page.tsx returns a match
- grep "getTranslations" src/app/[locale]/about/page.tsx returns a match
- grep "'about'" src/app/[locale]/about/page.tsx returns a match (namespace)
- grep "VerticalTimeline\|TimelineNode" src/app/[locale]/about/page.tsx returns a match (uses timeline components)
- grep "PageCTA" src/app/[locale]/about/page.tsx returns a match
- grep "font-heading" src/app/[locale]/about/page.tsx returns a match (Space Grotesk for headings)
- grep "text-accent" src/app/[locale]/about/page.tsx returns a match (bold metrics in accent)
- file does NOT contain 'use client' (Server Component)
- grep -i "hire me\|looking for\|job seeker\|available.*remote" src/app/[locale]/about/page.tsx returns ZERO matches (MODB-01, MODB-02)
- npm run build succeeds and about route shows circle icon (static)
</acceptance_criteria>
</task>

<task id="05-02-02">
<title>Build Approach page with 5 numbered prose sections</title>
<read_first>
- src/i18n/messages/en.json (approach namespace — verify it exists from 05-01)
- src/i18n/messages/it.json (approach namespace)
- src/components/layout/section.tsx (Section wrapper API)
- src/components/sections/page-cta.tsx (PageCTA API)
- src/app/[locale]/about/page.tsx (pattern reference from task 05-02-01)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (Approach Page layout contract)
- ../job-search-2026/selfrules-redesign/approach-page.md (source copy for verification)
</read_first>
<actions>
Create src/app/[locale]/approach/page.tsx as a Server Component:

1. **Boilerplate:** Same pattern as About — generateStaticParams, await params, setRequestLocale, getTranslations('approach').

2. **Headline + Intro (Section default width 720px):**
   - h1: title size clamp(28-36px), Space Grotesk 700, text-primary
   - Intro: body size (16px), Inter 400, text-secondary, line-height 1.7, 24px below headline

3. **5 numbered sections (48px from intro to first, 64px between sections):**
   - For each section (s1 through s5):
     - h2: subtitle size (20px), Space Grotesk 700, text-primary
     - Number in the title text itself (e.g., "1. Understand before you touch anything") — NO separate visual circle/line marker
     - Body: 16px, Inter 400, line-height 1.6, text-primary, multiple paragraphs per section
     - Bold inline metrics: t.rich() with `<bold>35%</bold>` → `<strong className="font-bold text-accent">`
   - 64px gap between sections (mb-16 or gap equivalent)

4. **Closing text (64px above PageCTA):**
   - Body size, Inter 400, text-primary, line-height 1.7

5. **PageCTA:**
   - text: t('cta.text')
   - primaryCta: { label: t('cta.primary'), href: '/work' } (or localized path)

IMPORTANT: This page is pure prose at 720px. No cards, no visual markers beyond numbered text. The content speaks about HOW HE THINKS and makes decisions (MODB-03).
</actions>
<acceptance_criteria>
- file src/app/[locale]/approach/page.tsx exists
- grep "generateStaticParams" src/app/[locale]/approach/page.tsx returns a match
- grep "setRequestLocale" src/app/[locale]/approach/page.tsx returns a match
- grep "'approach'" src/app/[locale]/approach/page.tsx returns a match (namespace)
- grep "PageCTA" src/app/[locale]/approach/page.tsx returns a match
- grep "font-heading" src/app/[locale]/approach/page.tsx returns a match (Space Grotesk for headings)
- file does NOT contain 'use client'
- grep -i "hire\|looking for\|available.*remote" src/app/[locale]/approach/page.tsx returns ZERO matches (MODB)
- npm run build succeeds and approach route shows circle icon (static)
</acceptance_criteria>
</task>

<task id="05-02-03">
<title>Visual verification — About and Approach pages</title>
<read_first>
- src/app/[locale]/about/page.tsx (just built)
- src/app/[locale]/approach/page.tsx (just built)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (layout contracts for both pages)
</read_first>
<actions>
Run `npm run dev` and verify the following in the browser:

**About page (/about and /it/about):**
1. Headline renders at title size with Space Grotesk
2. VerticalTimeline shows 4 nodes with vertical line on left, square nodes
3. Last node (QubicaAMF) has filled accent square
4. Career dates appear in monospace below company names
5. Bold metrics (35%, 99%+, -25%, 116 centers, 5 new per week) render in accent color
6. 3 principles render as sequential prose (no cards)
7. "Outside work" section is visually present
8. PageCTA renders with "Get in touch" / "Scrivimi" button
9. All text matches source copy EXACTLY (spot-check 3-5 phrases per locale)
10. No job-seeker framing visible

**Approach page (/approach and /it/approach):**
1. Headline and intro render correctly
2. 5 numbered sections with titles starting "1.", "2.", etc.
3. Bold metrics (35%) in accent color
4. Generous spacing between sections (64px)
5. PageCTA with "See the work" / "Guarda i lavori" button
6. All text at 720px max-width
7. Both locales render complete content

Fix any issues found before marking this task complete.
</actions>
<acceptance_criteria>
- npm run dev starts without errors
- /about renders without console errors
- /it/about renders with Italian content
- /approach renders without console errors
- /it/approach renders with Italian content
- npm run build succeeds with both routes showing circle icons (static)
</acceptance_criteria>
</task>
