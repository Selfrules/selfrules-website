---
wave: 1
depends_on: []
files_modified:
  - next.config.mjs
  - src/mdx-components.tsx
  - src/app/globals.css
  - src/components/sections/page-cta.tsx
  - src/i18n/messages/en.json
  - src/i18n/messages/it.json
  - package.json
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
  - MODB-01
  - MODB-02
  - MODB-03
  - MODB-04
---

# Plan 05-01: MDX Infrastructure, PageCTA Component, and Bilingual i18n Content

## Objective

Set up MDX compilation infrastructure (@next/mdx, @tailwindcss/typography), create the shared PageCTA component, and populate all bilingual i18n messages for the 6 inner pages (About, Work, Lab, Approach, Notes listing, Notes post). This is the foundation wave — all subsequent plans depend on having i18n content and MDX ready.

## Tasks

<task id="05-01-01">
<title>Install MDX and Typography dependencies</title>
<read_first>
- package.json (current dependencies)
- .planning/phases/05-inner-pages/05-RESEARCH.md (Installation section)
</read_first>
<actions>
Run the following commands:
```bash
npm install @next/mdx @mdx-js/loader @mdx-js/react @types/mdx
npm install -D @tailwindcss/typography remark-gfm
```
Verify all packages appear in package.json after install.
</actions>
<acceptance_criteria>
- grep "@next/mdx" package.json returns a match
- grep "@mdx-js/loader" package.json returns a match
- grep "@mdx-js/react" package.json returns a match
- grep "@types/mdx" package.json returns a match
- grep "@tailwindcss/typography" package.json returns a match
- grep "remark-gfm" package.json returns a match
- npm run build does not fail due to missing dependencies
</acceptance_criteria>
</task>

<task id="05-01-02">
<title>Configure next.config.mjs for MDX with Turbopack</title>
<read_first>
- next.config.mjs (current config)
- .planning/phases/05-inner-pages/05-RESEARCH.md (Pattern 6: next.config.mjs with MDX)
</read_first>
<actions>
Update next.config.mjs to wrap the existing config with createMDX:
```javascript
import createMDX from '@next/mdx';

// Keep existing nextConfig, add pageExtensions: ['js', 'jsx', 'md', 'mdx', 'ts', 'tsx']
// Wrap with withMDX using remarkPlugins: ['remark-gfm'] (string name for Turbopack)
```
CRITICAL: Use string plugin names, not imported functions (Turbopack requirement).
</actions>
<acceptance_criteria>
- grep "createMDX" next.config.mjs returns a match
- grep "pageExtensions" next.config.mjs returns a match
- grep "'remark-gfm'" next.config.mjs returns a match (string, not import)
- grep "withMDX" next.config.mjs returns a match
- npm run build succeeds with MDX config
</acceptance_criteria>
</task>

<task id="05-01-03">
<title>Create mdx-components.tsx</title>
<read_first>
- .planning/phases/05-inner-pages/05-RESEARCH.md (Pattern 7: mdx-components.tsx)
</read_first>
<actions>
Create src/mdx-components.tsx (required by @next/mdx for App Router):
```typescript
import type { MDXComponents } from 'mdx/types';

export function useMDXComponents(): MDXComponents {
  return {};
}
```
This file is REQUIRED. Without it, MDX compilation fails (Pitfall 1 from RESEARCH.md).
</actions>
<acceptance_criteria>
- file src/mdx-components.tsx exists
- grep "useMDXComponents" src/mdx-components.tsx returns a match
- grep "MDXComponents" src/mdx-components.tsx returns a match
</acceptance_criteria>
</task>

<task id="05-01-04">
<title>Register @tailwindcss/typography plugin in globals.css</title>
<read_first>
- src/app/globals.css (current content)
- .planning/phases/05-inner-pages/05-RESEARCH.md (Pattern 5: @tailwindcss/typography)
</read_first>
<actions>
Add the following directive to globals.css (Tailwind v4 CSS-first plugin registration):
```css
@plugin "@tailwindcss/typography";
```
Place it near other @plugin or @import directives at the top of the file.
</actions>
<acceptance_criteria>
- grep '@plugin "@tailwindcss/typography"' src/app/globals.css returns a match
- npm run build succeeds (typography plugin recognized)
</acceptance_criteria>
</task>

<task id="05-01-05">
<title>Create PageCTA component</title>
<read_first>
- src/components/sections/ (existing section components)
- src/components/ui/button.tsx (Button component API)
- src/components/layout/section.tsx (Section wrapper API)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (PageCTA section)
- .planning/phases/05-inner-pages/05-RESEARCH.md (Pattern 4: PageCTA Component)
</read_first>
<actions>
Create src/components/sections/page-cta.tsx:
- Props interface: text (string), primaryCta ({label: string, href: string}), secondaryCta? ({label: string, href: string})
- Render inside Section wrapper (default 720px max-width)
- 80px top margin (mt-[80px])
- Text: body size (16px), leading-[1.7], text-primary
- 1-2 Button components: primary variant for first, secondary for optional second
- Buttons in flex row with gap-4
- Server Component (no 'use client')
</actions>
<acceptance_criteria>
- file src/components/sections/page-cta.tsx exists
- grep "PageCTA" src/components/sections/page-cta.tsx returns a match
- grep "primaryCta" src/components/sections/page-cta.tsx returns a match
- grep "secondaryCta" src/components/sections/page-cta.tsx returns a match
- grep "mt-\[80px\]" src/components/sections/page-cta.tsx returns a match
- file does NOT contain 'use client'
- npm run build succeeds
</acceptance_criteria>
</task>

<task id="05-01-06">
<title>Populate EN i18n messages for all 6 inner pages</title>
<read_first>
- src/i18n/messages/en.json (current structure)
- ../job-search-2026/selfrules-redesign/about-page.md (EN section)
- ../job-search-2026/selfrules-redesign/work-page.md (EN section)
- ../job-search-2026/selfrules-redesign/lab-page.md (EN section)
- ../job-search-2026/selfrules-redesign/approach-page.md (EN section)
- ../job-search-2026/selfrules-redesign/blog-prototype-in-code.md (EN section — only metadata, not full text)
- ../job-search-2026/selfrules-redesign/microcopy.md (EN section)
- .planning/phases/05-inner-pages/05-RESEARCH.md (i18n Message Structure example)
</read_first>
<actions>
Add the following namespaces to en.json. Copy text VERBATIM from source files (COPY-01).

**about** namespace:
- headline, career.selfrules (company, dates, text with rich text tags for bold metrics), career.flowing, career.leadsbridge, career.qubicamf
- beliefs.belief1 (title, text), beliefs.belief2, beliefs.belief3
- outside.text
- cta.text, cta.primary ("Get in touch")

**work** namespace:
- headline, intro
- cases.payments (tag, title, metric, preview)
- cases.cashless (tag, title, metric, preview, note)
- cta.text, cta.primary ("About me"), cta.secondary ("Get in touch")

**lab** namespace:
- headline, intro
- casahunter (oneLiner, problem, whatItDoes, productDecision, stack, status, links)
- moneymind (title, oneLiner)
- cta.text, cta.primary ("See the work")

**approach** namespace:
- headline, intro
- sections.s1 through s5 (title, text — with rich text tags for bold metrics where needed)
- closing.text
- cta.text, cta.primary ("See the work")

**notes** namespace:
- headline ("Notes")
- emptyState
- posts.why-i-prototype-in-code (title, date, excerpt)

For inline bold metrics (e.g., **35%**, **99%+**), use next-intl rich text format: `<bold>35%</bold>` in the JSON string. This is a technical adaptation (markdown -> JSX), not a copy modification.

CRITICAL: Do NOT include availability tags, CV download links, or job-seeker framing in any inner page content (MODB-01, MODB-02). Verify: grep for "available", "CV", "hire", "looking for" in the new JSON entries — zero matches expected.
</actions>
<acceptance_criteria>
- grep '"about"' src/i18n/messages/en.json returns a match
- grep '"work"' src/i18n/messages/en.json returns a match
- grep '"lab"' src/i18n/messages/en.json returns a match
- grep '"approach"' src/i18n/messages/en.json returns a match
- grep '"notes"' src/i18n/messages/en.json returns a match
- grep "Better products happen" src/i18n/messages/en.json returns a match (About headline verbatim)
- grep "How I work, demonstrated" src/i18n/messages/en.json returns a match (Work headline verbatim)
- grep "Same thinking, applied to my own problems" src/i18n/messages/en.json returns a match (Lab headline verbatim)
- grep "How I make product decisions" src/i18n/messages/en.json returns a match (Approach headline verbatim)
- grep -i "hire me\|looking for.*role\|job seeker" src/i18n/messages/en.json returns ZERO matches (MODB-01)
- grep -i "available.*remote\|download cv" src/i18n/messages/en.json returns ZERO matches in about/work/lab/approach/notes namespaces (MODB-02)
- JSON is valid (node -e "JSON.parse(require('fs').readFileSync('src/i18n/messages/en.json'))" succeeds)
</acceptance_criteria>
</task>

<task id="05-01-07">
<title>Populate IT i18n messages for all 6 inner pages</title>
<read_first>
- src/i18n/messages/it.json (current structure)
- ../job-search-2026/selfrules-redesign/about-page.md (IT section)
- ../job-search-2026/selfrules-redesign/work-page.md (IT section)
- ../job-search-2026/selfrules-redesign/lab-page.md (IT section)
- ../job-search-2026/selfrules-redesign/approach-page.md (IT section)
- ../job-search-2026/selfrules-redesign/blog-prototype-in-code.md (IT section — only metadata)
- ../job-search-2026/selfrules-redesign/microcopy.md (IT section)
</read_first>
<actions>
Add the same namespace structure as en.json but with IT copy. Copy text VERBATIM from source files (COPY-02).

Mirror the exact structure of en.json for all 5 namespaces (about, work, lab, approach, notes). Same keys, Italian values.

Use `<bold>35%</bold>` rich text format for inline bold metrics, matching the EN structure.

CRITICAL: Copy is DEFINITIVO. No rewriting, no summarizing, no improving (COPY-01).
</actions>
<acceptance_criteria>
- grep '"about"' src/i18n/messages/it.json returns a match
- grep '"work"' src/i18n/messages/it.json returns a match
- grep '"lab"' src/i18n/messages/it.json returns a match
- grep '"approach"' src/i18n/messages/it.json returns a match
- grep '"notes"' src/i18n/messages/it.json returns a match
- grep "Prodotti migliori nascono" src/i18n/messages/it.json returns a match (About headline verbatim)
- grep "Come lavoro, dimostrato" src/i18n/messages/it.json returns a match (Work headline verbatim)
- grep "Lo stesso modo di pensare" src/i18n/messages/it.json returns a match (Lab headline verbatim)
- grep "Come prendo decisioni di prodotto" src/i18n/messages/it.json returns a match (Approach headline verbatim)
- grep -i "hire\|looking for.*role\|job seeker\|cercando.*ruolo" src/i18n/messages/it.json returns ZERO matches (MODB-01)
- JSON is valid (node -e "JSON.parse(require('fs').readFileSync('src/i18n/messages/it.json'))" succeeds)
</acceptance_criteria>
</task>

<task id="05-01-08">
<title>Build verification</title>
<read_first>
- package.json (build script)
</read_first>
<actions>
Run `npm run build` and verify:
1. No TypeScript errors
2. No MDX compilation errors
3. All existing routes still render (circle icons in build output, not lambda)
4. The i18n JSON files parse correctly

If build fails, diagnose and fix before proceeding.
</actions>
<acceptance_criteria>
- npm run build exits with code 0
- Build output shows circle icons (static) for all existing routes
- No TypeScript errors in build output
- No "useMDXComponents" errors in build output
</acceptance_criteria>
</task>
