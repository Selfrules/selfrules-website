---
wave: 3
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
  - 05-03-PLAN.md
files_modified:
  - src/app/[locale]/notes/page.tsx
  - src/app/[locale]/notes/why-i-prototype-in-code/page.tsx
  - src/app/[locale]/notes/why-i-prototype-in-code/content.en.mdx
  - src/app/[locale]/notes/why-i-prototype-in-code/content.it.mdx
autonomous: true
requirements:
  - PAGE-05
  - PAGE-06
  - COPY-01
  - MODB-01
  - MODB-03
---

# Plan 05-04: Notes Listing, Notes Post, and Phase Visual Verification

## Objective

Build the Notes listing page (headline, post list with date/title/excerpt, empty state) and the Notes post page (MDX blog post "Why I prototype in code" with locale-based loading and @tailwindcss/typography prose styling). Then perform a full phase visual verification checkpoint across all 6 inner pages in both locales.

## Tasks

<task id="05-04-01">
<title>Create Notes listing page</title>
<read_first>
- src/i18n/messages/en.json (notes namespace — verify it exists from 05-01)
- src/i18n/messages/it.json (notes namespace)
- src/components/layout/section.tsx (Section wrapper API)
- src/app/[locale]/about/page.tsx (pattern reference for boilerplate)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (Notes Listing layout contract)
- .planning/phases/05-inner-pages/05-RESEARCH.md (Blog Listing Post Entry code example)
</read_first>
<actions>
Create src/app/[locale]/notes/page.tsx as a Server Component:

1. **Boilerplate:** generateStaticParams, await params, setRequestLocale, getTranslations('notes').

2. **Headline (Section default width 720px):**
   - h1: title size clamp(28-36px), Space Grotesk 700, text-primary
   - Content: t('headline') — "Notes" / "Note"

3. **Post list (720px max-width, 48px below headline):**
   - Define posts array inline (for v1 with 1 post, hardcoded is fine):
     ```typescript
     const posts = [
       {
         slug: 'why-i-prototype-in-code',
         title: t('posts.why-i-prototype-in-code.title'),
         date: t('posts.why-i-prototype-in-code.date'),
         excerpt: t('posts.why-i-prototype-in-code.excerpt'),
       },
     ];
     ```
   - For each post, render:
     - Date: label role (14px mono, uppercase, tracking-wide, text-secondary)
     - Title: Link to `/notes/${post.slug}` (or localized). Style: subtitle size (20px), Inter 400 (body font, NOT Space Grotesk), text-primary, hover text-accent (150ms transition)
     - Excerpt: 16px, Inter 400, text-secondary, 4px below title
   - 48px vertical gap between entries (space-y-12)
   - Use next-intl Link component for locale-aware routing

4. **Empty state (conditional, for future when posts array is empty):**
   - Render t('emptyState') in body size (16px), Inter 400, text-secondary
   - Left-aligned at 720px

NOTE: With 1 post, the page will be sparse. This is intentional (D-07). No pagination, no categories, no search.
</actions>
<acceptance_criteria>
- file src/app/[locale]/notes/page.tsx exists
- grep "generateStaticParams" src/app/[locale]/notes/page.tsx returns a match
- grep "setRequestLocale" src/app/[locale]/notes/page.tsx returns a match
- grep "'notes'" src/app/[locale]/notes/page.tsx returns a match (namespace)
- grep "why-i-prototype-in-code" src/app/[locale]/notes/page.tsx returns a match (post slug)
- grep "emptyState\|empty" src/app/[locale]/notes/page.tsx returns a match (empty state handled)
- grep "font-mono" src/app/[locale]/notes/page.tsx returns a match (date styling)
- file does NOT contain 'use client'
- npm run build succeeds and notes route shows circle icon (static)
</acceptance_criteria>
</task>

<task id="05-04-02">
<title>Create MDX content files for Notes post (EN + IT)</title>
<read_first>
- ../job-search-2026/selfrules-redesign/blog-prototype-in-code.md (FULL source copy EN + IT)
- .planning/phases/05-inner-pages/05-RESEARCH.md (Pattern 3: MDX Metadata via Exports)
- .planning/phases/05-inner-pages/05-CONTEXT.md (D-08: single slug, D-09: prose styling)
</read_first>
<actions>
Create two MDX files:

**src/app/[locale]/notes/why-i-prototype-in-code/content.en.mdx:**
```mdx
export const metadata = {
  title: 'Why I prototype in code',
  date: '2026-03-22',
  excerpt: 'A Product Manager who prototypes in code makes different decisions.',
  locale: 'en',
}

[FULL EN BODY TEXT FROM SOURCE — COPY VERBATIM]
```

**src/app/[locale]/notes/why-i-prototype-in-code/content.it.mdx:**
```mdx
export const metadata = {
  title: 'Perché prototipo in codice',
  date: '2026-03-22',
  excerpt: 'Un Product Manager che prototipa in codice prende decisioni diverse.',
  locale: 'it',
}

[FULL IT BODY TEXT FROM SOURCE — COPY VERBATIM]
```

CRITICAL requirements:
- Use `export const metadata` pattern, NOT YAML frontmatter (Pattern 3 from RESEARCH.md)
- Copy the FULL blog post body text VERBATIM from the source file (COPY-01)
- Use `---` for section breaks (renders as `<hr>` in MDX)
- Inline code: use backticks for `external_payment_type`
- Bold text: use **bold** markdown syntax (prose-strong will handle accent color in the wrapper)
- Single slug for both languages: `why-i-prototype-in-code` (D-08)
- Do NOT create separate IT slug route (Pitfall 5)
</actions>
<acceptance_criteria>
- file src/app/[locale]/notes/why-i-prototype-in-code/content.en.mdx exists
- file src/app/[locale]/notes/why-i-prototype-in-code/content.it.mdx exists
- grep "export const metadata" src/app/[locale]/notes/why-i-prototype-in-code/content.en.mdx returns a match
- grep "export const metadata" src/app/[locale]/notes/why-i-prototype-in-code/content.it.mdx returns a match
- grep "Most PMs don't write code" src/app/[locale]/notes/why-i-prototype-in-code/content.en.mdx returns a match (opening line verbatim)
- grep "La maggior parte dei PM non scrive codice" src/app/[locale]/notes/why-i-prototype-in-code/content.it.mdx returns a match (opening line verbatim)
- grep "external_payment_type" src/app/[locale]/notes/why-i-prototype-in-code/content.en.mdx returns a match (inline code preserved)
- grep "external_payment_type" src/app/[locale]/notes/why-i-prototype-in-code/content.it.mdx returns a match
</acceptance_criteria>
</task>

<task id="05-04-03">
<title>Create Notes post page.tsx with locale-based MDX loading</title>
<read_first>
- src/app/[locale]/notes/why-i-prototype-in-code/content.en.mdx (just created)
- src/app/[locale]/notes/why-i-prototype-in-code/content.it.mdx (just created)
- src/components/layout/section.tsx (Section wrapper API)
- .planning/phases/05-inner-pages/05-RESEARCH.md (Pattern 2: Blog Post with Locale-Based MDX Loading)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (Notes Post layout contract)
</read_first>
<actions>
Create src/app/[locale]/notes/why-i-prototype-in-code/page.tsx as a Server Component:

1. **Boilerplate:** generateStaticParams returning [{locale:'en'},{locale:'it'}]. Await params, setRequestLocale.

2. **Dynamic MDX import:**
   ```typescript
   const { default: Content, metadata } = await import(`./content.${locale}.mdx`);
   ```

3. **Post header (Section default width 720px):**
   - article wrapper with semantic HTML
   - h1: title size clamp(28-36px), Space Grotesk 700, text-primary — from metadata.title
   - time element: label role (14px mono, uppercase, tracking-wide, text-secondary), 8px below title — from metadata.date

4. **Prose body (48px below header):**
   - Wrapper div with @tailwindcss/typography classes:
     ```
     prose prose-invert max-w-none leading-[1.7]
     prose-headings:font-heading prose-headings:text-[var(--color-text-primary)]
     prose-p:text-[var(--color-text-primary)]
     prose-a:text-[var(--color-text-primary)] prose-a:underline hover:prose-a:text-[var(--color-accent)]
     prose-strong:text-[var(--color-accent)] prose-strong:font-bold
     prose-code:font-mono prose-code:text-sm prose-code:bg-[var(--color-bg-surface)] prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded-none
     prose-hr:border-[var(--color-border-default)] prose-hr:my-12
     ```
   - Max-width 720px handled by Section wrapper
   - Render `<Content />` inside the prose wrapper
   - NOTE on prose-strong:text-accent: In this blog post, bold text IS metric emphasis and key phrases. Accent-on-bold is correct here. This is DIFFERENT from About/Approach pages where bold metrics are handled via explicit spans.

5. **No pagination, no sidebar, no reading time, no "next/previous post"** — minimal.
</actions>
<acceptance_criteria>
- file src/app/[locale]/notes/why-i-prototype-in-code/page.tsx exists
- grep "generateStaticParams" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match
- grep "setRequestLocale" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match
- grep "content\.\${locale}\.mdx\|content\.en\.mdx" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match (dynamic MDX import)
- grep "prose" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match (typography classes)
- grep "prose-invert" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match
- grep "<article" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match (semantic HTML)
- grep "<time" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match (date element)
- grep "font-heading" src/app/[locale]/notes/why-i-prototype-in-code/page.tsx returns a match (Space Grotesk for title)
- file does NOT contain 'use client'
- npm run build succeeds and notes/why-i-prototype-in-code route shows circle icon (static)
</acceptance_criteria>
</task>

<task id="05-04-04">
<title>Full Phase 5 visual verification checkpoint</title>
<read_first>
- .planning/phases/05-inner-pages/05-UI-SPEC.md (all layout contracts)
- .planning/REQUIREMENTS.md (Phase 5 requirements: PAGE-01 through PAGE-06, COPY-01/02/03, MODB-01/02/03/04)
- ../job-search-2026/selfrules-redesign/about-page.md (spot-check copy)
- ../job-search-2026/selfrules-redesign/work-page.md (spot-check copy)
- ../job-search-2026/selfrules-redesign/approach-page.md (spot-check copy)
- ../job-search-2026/selfrules-redesign/blog-prototype-in-code.md (spot-check copy)
</read_first>
<actions>
Run `npm run dev` and perform comprehensive verification across all 6 pages in both locales (12 page views total).

**Checklist per page (EN + IT):**

1. **About (/about, /it/about):**
   - [ ] Headline matches source copy exactly
   - [ ] 4 career blocks with VerticalTimeline (square nodes, last accent-filled)
   - [ ] Bold metrics (35%, 99%+, etc.) in accent color
   - [ ] 3 principles render as prose
   - [ ] "Outside work" section present
   - [ ] PageCTA with "Get in touch" / "Scrivimi"

2. **Work (/work, /it/work):**
   - [ ] 2 CaseStudyCards, NOT clickable
   - [ ] Card 2 has [DRAFT]/[BOZZA] note
   - [ ] No Card 3 (LeadsBridge)
   - [ ] PageCTA with 2 buttons

3. **Lab (/lab, /it/lab):**
   - [ ] CasaHunter fully expanded inline
   - [ ] Stack tags rendered
   - [ ] MoneyMind "coming soon" present
   - [ ] PageCTA present

4. **Approach (/approach, /it/approach):**
   - [ ] 5 numbered sections with 64px spacing
   - [ ] Bold metrics in accent
   - [ ] 720px max-width throughout
   - [ ] PageCTA present

5. **Notes listing (/notes, /it/notes):**
   - [ ] "Notes" / "Note" headline
   - [ ] 1 post entry with date (mono), title (link), excerpt
   - [ ] Title links to correct post URL

6. **Notes post (/notes/why-i-prototype-in-code, /it/notes/why-i-prototype-in-code):**
   - [ ] Title and date render correctly
   - [ ] Prose body with @tailwindcss/typography styling
   - [ ] Section breaks (---) render as subtle hr lines
   - [ ] Inline code `external_payment_type` has mono styling
   - [ ] Bold text in accent color
   - [ ] 720px max-width
   - [ ] EN and IT load correct locale content

**Cross-page checks:**
- [ ] No "job seeker" framing on any page (MODB-01)
- [ ] No availability/CV references outside Contact+Footer (MODB-02)
- [ ] All pages use Space Grotesk for headings
- [ ] 0px border-radius on all elements
- [ ] All pages are statically rendered (npm run build shows circle icons)

**Build verification:**
```bash
npm run build
```
All 12 routes (6 pages x 2 locales) must show circle icons (static rendering).

Fix any issues found. If copy does not match source, correct the i18n JSON files. If layout does not match UI-SPEC, adjust the page component.
</actions>
<acceptance_criteria>
- npm run build exits with code 0
- All 6 page routes show circle icons in build output (static, not dynamic)
- /about, /work, /lab, /approach, /notes, /notes/why-i-prototype-in-code all render without console errors
- /it/about, /it/work, /it/lab, /it/approach, /it/notes, /it/notes/why-i-prototype-in-code all render without console errors
- grep -r "hire me\|looking for.*role\|job seeker" src/app/[locale]/ returns ZERO matches (MODB-01)
- grep -r "available.*remote\|download cv" src/app/[locale]/about/ src/app/[locale]/work/ src/app/[locale]/lab/ src/app/[locale]/approach/ src/app/[locale]/notes/ returns ZERO matches (MODB-02)
- No TypeScript errors in build output
</acceptance_criteria>
</task>
