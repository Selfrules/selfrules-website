---
phase: 03-ui-primitives
plan: 02
type: execute
wave: 2
depends_on: [03-01-PLAN.md]
files_modified:
  - src/components/ui/MetricCard.tsx
  - src/components/ui/CaseStudyCard.tsx
  - src/components/ui/ProjectCard.tsx
  - src/components/ui/BlinkingCursor.tsx
  - src/components/ui/TerminalPrompt.tsx
  - src/components/ui/CountUpNumber.tsx
  - src/components/ui/VerticalTimeline.tsx
  - src/components/ui/TimelineNode.tsx
  - src/app/[locale]/dev/components/page.tsx
autonomous: true
requirements: [UICM-02, UICM-03, UICM-04, UICM-05]

must_haves:
  truths:
    - "MetricCard renders accent-colored Space Grotesk number with clamp(40px,5vw,72px) sizing"
    - "MetricCard grid uses gap-[1px] shared border pattern with container bg-[var(--border-default)]"
    - "MetricCard hover uses outline approach for accent border effect within gap-[1px] grid"
    - "CaseStudyCard has flex-row desktop (metric right) and flex-col mobile (metric below) layout"
    - "CaseStudyCard padding is p-8 on mobile, md:p-12 on desktop (D-02)"
    - "CaseStudyCard hover has both border-accent/40 AND translate-y(-2px)"
    - "ProjectCard has status dot (only exception to 0px border-radius) with text label for a11y"
    - "BlinkingCursor is a Client Component with CSS step-end animation and prefers-reduced-motion support"
    - "TerminalPrompt renders 'mattia@selfrules ~ $' + BlinkingCursor block"
    - "CountUpNumber is a Client Component with IntersectionObserver trigger and requestAnimationFrame animation"
    - "VerticalTimeline renders a 2px vertical line on the left in border-default color"
    - "TimelineNode has square nodes (0px border-radius), active node filled accent"
    - "TimelineNode content block has bg-surface, p-6, and hover border-accent/50"
    - "/dev/components route shows all components and returns 404 in production"
    - "All Server Components (MetricCard, CaseStudyCard, ProjectCard, TerminalPrompt, VerticalTimeline, TimelineNode) have NO 'use client' directive"
  artifacts:
    - path: "src/components/ui/MetricCard.tsx"
      provides: "Accent number metric card for gap-[1px] grid layout"
      exports: ["MetricCard"]
    - path: "src/components/ui/CaseStudyCard.tsx"
      provides: "Responsive flex card with tag, title, preview, and large metric number"
      exports: ["CaseStudyCard"]
    - path: "src/components/ui/ProjectCard.tsx"
      provides: "Side project card with status dot, tech tags, and optional link"
      exports: ["ProjectCard"]
    - path: "src/components/ui/BlinkingCursor.tsx"
      provides: "Client Component: animated cursor in block or underscore variants"
      exports: ["BlinkingCursor"]
    - path: "src/components/ui/TerminalPrompt.tsx"
      provides: "Terminal prompt line with blinking cursor"
      exports: ["TerminalPrompt"]
    - path: "src/components/ui/CountUpNumber.tsx"
      provides: "Client Component: scroll-triggered animated count-up number"
      exports: ["CountUpNumber"]
    - path: "src/components/ui/VerticalTimeline.tsx"
      provides: "Container with vertical line for timeline nodes"
      exports: ["VerticalTimeline"]
    - path: "src/components/ui/TimelineNode.tsx"
      provides: "Single timeline entry with square node, connector, and content block"
      exports: ["TimelineNode"]
    - path: "src/app/[locale]/dev/components/page.tsx"
      provides: "Dev-only verification page showing all Phase 3 components"
      contains: "NODE_ENV"
  key_links:
    - from: "src/components/ui/CaseStudyCard.tsx"
      to: "src/components/ui/Tag.tsx"
      via: "import Tag"
      pattern: "import.*Tag.*from.*ui/Tag"
    - from: "src/components/ui/ProjectCard.tsx"
      to: "src/components/ui/Tag.tsx"
      via: "import Tag"
      pattern: "import.*Tag.*from.*ui/Tag"
    - from: "src/components/ui/TerminalPrompt.tsx"
      to: "src/components/ui/BlinkingCursor.tsx"
      via: "import BlinkingCursor"
      pattern: "import.*BlinkingCursor.*from.*ui/BlinkingCursor"
    - from: "src/app/[locale]/dev/components/page.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import Button"
      pattern: "import.*Button.*from.*ui/Button"
    - from: "src/app/[locale]/dev/components/page.tsx"
      to: "src/components/ui/MetricCard.tsx"
      via: "import MetricCard"
      pattern: "import.*MetricCard.*from.*ui/MetricCard"
---

<objective>
Build the remaining 8 components (MetricCard, CaseStudyCard, ProjectCard, BlinkingCursor, TerminalPrompt, CountUpNumber, VerticalTimeline, TimelineNode) and create the /dev/components verification route to visually validate all Phase 3 components.

Purpose: Complete the component library that Phase 4 (Homepage) and Phase 5 (Inner Pages) compose. The verification route enables visual validation of every component against the 32 success criteria in 03-UI-SPEC.md.
Output: 8 new components + 1 dev verification page. After this plan, Phase 3 is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-primitives/03-CONTEXT.md
@.planning/phases/03-ui-primitives/03-RESEARCH.md
@.planning/phases/03-ui-primitives/03-UI-SPEC.md
@DESIGN-UPDATE-v25.md
</context>

<task id="03-02-T1" title="Create MetricCard component">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (MetricCard: Interaction Contracts hover, Layout Contracts grid, Component API, Typography for metric number)
- .planning/phases/03-ui-primitives/03-RESEARCH.md (Pattern 3: MetricCard Grid Container, Pitfall 1: grid hover, Pitfall 2: clamp syntax)
- src/app/globals.css (verify --bg-primary, --border-default CSS variables exist)
</read_first>

<actions>
1. Create `src/components/ui/MetricCard.tsx`:
   - Import `cn` from `@/lib/utils`
   - Interface `MetricCardProps`: `number: string`, `label: string`, `context: string`, `className?: string`
   - Cell background: `bg-primary` (fills the grid gap-[1px] cell)
   - Padding: `p-8` (32px)
   - Hover effect: Use outline approach for gap-[1px] grid compatibility:
     `outline outline-0 hover:outline-1 hover:outline-accent/40 hover:-outline-offset-1`
     with `transition-all duration-200`
   - Number: `font-heading font-bold text-accent text-[clamp(40px,5vw,72px)] leading-none`
     CRITICAL: Use `font-heading` (Space Grotesk), NOT `font-mono` (per DESIGN-UPDATE-v25)
   - Label: `text-[13px] text-primary mt-4`
   - Context: `text-[13px] text-tertiary mt-1`
   - NO `'use client'`
   - NO border-radius
2. Add a comment above the component documenting the grid container pattern:
   ```
   // Usage: Wrap MetricCard components in a grid container:
   // <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-[1px] bg-[var(--border-default)] border border-default">
   //   <MetricCard ... />
   // </div>
   ```
</actions>

<acceptance_criteria>
- grep -q "export function MetricCard" src/components/ui/MetricCard.tsx
- grep -q "font-heading" src/components/ui/MetricCard.tsx
- grep -q "font-bold" src/components/ui/MetricCard.tsx
- grep -q "text-accent" src/components/ui/MetricCard.tsx
- grep -q "clamp(40px,5vw,72px)" src/components/ui/MetricCard.tsx
- grep -q "leading-none" src/components/ui/MetricCard.tsx
- grep -q "bg-primary" src/components/ui/MetricCard.tsx
- grep -q "p-8" src/components/ui/MetricCard.tsx
- grep -q "outline" src/components/ui/MetricCard.tsx
- grep -q "accent/40" src/components/ui/MetricCard.tsx
- grep -q "duration-200" src/components/ui/MetricCard.tsx
- grep -q "text-tertiary" src/components/ui/MetricCard.tsx
- grep -q "gap-\[1px\]" src/components/ui/MetricCard.tsx (in usage comment)
- grep -qv "use client" src/components/ui/MetricCard.tsx
- grep -qv "font-mono.*clamp" src/components/ui/MetricCard.tsx (metric number must NOT use font-mono)
</acceptance_criteria>
</task>

<task id="03-02-T2" title="Create CaseStudyCard component">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (CaseStudyCard: Interaction Contracts, Layout Contracts, Component API)
- .planning/phases/03-ui-primitives/03-CONTEXT.md (D-02: responsive padding p-8 mobile, md:p-12 desktop)
- src/components/ui/Tag.tsx (verify Tag is available for import)
</read_first>

<actions>
1. Create `src/components/ui/CaseStudyCard.tsx`:
   - Import `cn` from `@/lib/utils` and `Tag` from `@/components/ui/Tag`
   - Interface `CaseStudyCardProps`: `tag: string`, `title: string`, `preview: string`, `metric: string`, `metricLabel?: string`, `className?: string`
   - Outer div: `border border-default p-8 md:p-12 flex flex-col md:flex-row md:items-center gap-6`
   - Hover: `transition-all duration-200 hover:border-accent/40 hover:-translate-y-[2px]`
   - Left side (flex-1):
     - `<Tag>{tag}</Tag>` at top
     - Title: `<h3>` with `text-xl font-bold text-primary font-heading mt-3`
     - Preview: `<p>` with `text-base text-secondary mt-2`
   - Right side (shrink-0, md:text-right):
     - Metric number: `font-heading font-bold text-accent text-[clamp(40px,5vw,72px)] leading-none`
       CRITICAL: Use `font-heading` (Space Grotesk), NOT `font-mono`
     - Optional metric label: `text-[13px] text-tertiary mt-1` (only if metricLabel provided)
   - NO `'use client'`
   - NO border-radius
</actions>

<acceptance_criteria>
- grep -q "export function CaseStudyCard" src/components/ui/CaseStudyCard.tsx
- grep -q "import.*Tag.*from" src/components/ui/CaseStudyCard.tsx
- grep -q "flex-col md:flex-row" src/components/ui/CaseStudyCard.tsx
- grep -q "p-8 md:p-12" src/components/ui/CaseStudyCard.tsx
- grep -q "hover:border-accent/40" src/components/ui/CaseStudyCard.tsx
- grep -q "hover:-translate-y-\[2px\]" src/components/ui/CaseStudyCard.tsx
- grep -q "font-heading" src/components/ui/CaseStudyCard.tsx
- grep -q "clamp(40px,5vw,72px)" src/components/ui/CaseStudyCard.tsx
- grep -q "text-accent" src/components/ui/CaseStudyCard.tsx
- grep -q "metricLabel" src/components/ui/CaseStudyCard.tsx
- grep -q "shrink-0" src/components/ui/CaseStudyCard.tsx
- grep -qv "use client" src/components/ui/CaseStudyCard.tsx
</acceptance_criteria>
</task>

<task id="03-02-T3" title="Create ProjectCard component">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (ProjectCard: Layout Contracts, Interaction Contracts hover, status dot exception)
- src/components/ui/Tag.tsx (verify available)
</read_first>

<actions>
1. Create `src/components/ui/ProjectCard.tsx`:
   - Import `cn` from `@/lib/utils`, `Tag` from `@/components/ui/Tag`, `Link` from `next/link`
   - Interface `ProjectCardProps`: `title: string`, `description: string`, `techStack: string[]`, `status: 'active' | 'shipped' | 'coming-soon'`, `statusLabel: string`, `href?: string`, `className?: string`
   - Outer div: `border border-default p-8 transition-[border-color] duration-200 hover:border-accent/40`
   - NO translate-y hover (ProjectCard only has border hover, per UI-SPEC)
   - Row 1: Title (`font-heading font-bold text-xl text-primary`) + status dot + status label inline
   - Status dot: `w-2 h-2 rounded-full inline-block` with color map:
     - active: `bg-accent`
     - shipped: `bg-[#4ADE80]`
     - coming-soon: `bg-secondary`
     EXCEPTION: rounded-full is the ONLY 0px border-radius exception (status dots)
   - Status label: `text-[13px] text-secondary ml-2` next to dot
   - Row 2: One-liner `text-base text-secondary mt-2`
   - Row 3: Tech stack tags `mt-4 flex flex-wrap gap-2` with `<Tag>` for each tech item
   - Row 4 (optional): Link `mt-4 text-accent hover:underline` with arrow suffix if href provided
   - NO `'use client'`
</actions>

<acceptance_criteria>
- grep -q "export function ProjectCard" src/components/ui/ProjectCard.tsx
- grep -q "import.*Tag.*from" src/components/ui/ProjectCard.tsx
- grep -q "rounded-full" src/components/ui/ProjectCard.tsx
- grep -q "bg-accent" src/components/ui/ProjectCard.tsx
- grep -q "bg-\[#4ADE80\]" src/components/ui/ProjectCard.tsx
- grep -q "hover:border-accent/40" src/components/ui/ProjectCard.tsx
- grep -q "font-heading" src/components/ui/ProjectCard.tsx
- grep -q "statusLabel" src/components/ui/ProjectCard.tsx
- grep -q "techStack" src/components/ui/ProjectCard.tsx
- grep -q "flex-wrap" src/components/ui/ProjectCard.tsx
- grep -q "p-8" src/components/ui/ProjectCard.tsx
- grep -qv "translate-y" src/components/ui/ProjectCard.tsx (no translate-y on ProjectCard)
- grep -qv "use client" src/components/ui/ProjectCard.tsx
</acceptance_criteria>
</task>

<task id="03-02-T4" title="Create BlinkingCursor client component">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (BlinkingCursor: Component API, Animation timing, reduced motion)
</read_first>

<actions>
1. Create `src/components/ui/BlinkingCursor.tsx`:
   - Add `'use client'` directive at top (required for CSS animation class + reduced motion check)
   - Interface `BlinkingCursorProps`: `type: 'block' | 'underscore'`, `className?: string`
   - Export named function `BlinkingCursor`
   - Block variant: renders a small filled `<span>` with `inline-block w-[8px] h-[1.1em] bg-accent align-middle ml-1`
   - Underscore variant: renders `<span>` with text `_` and `text-accent`
   - Both variants get animation class: `animate-blink`
   - Add CSS keyframes using Tailwind v4 inline approach or a `<style>` tag:
     ```css
     @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
     ```
     With `animation: blink 1s step-end infinite`
   - Reduced motion: use `motion-safe:animate-blink` or check with `@media (prefers-reduced-motion: reduce)` to disable animation (cursor stays visible at full opacity)
   - ALTERNATIVE: Define the animation inline via Tailwind arbitrary values or add to globals.css @theme
2. Ensure the keyframes animation is defined (either in globals.css or inline).
</actions>

<acceptance_criteria>
- grep -q "use client" src/components/ui/BlinkingCursor.tsx
- grep -q "export function BlinkingCursor" src/components/ui/BlinkingCursor.tsx
- grep -q "block.*underscore" src/components/ui/BlinkingCursor.tsx
- grep -q "bg-accent" src/components/ui/BlinkingCursor.tsx
- grep -q "text-accent" src/components/ui/BlinkingCursor.tsx
- grep -q "blink" src/components/ui/BlinkingCursor.tsx
- grep -q "reduced-motion\|motion-safe\|motion-reduce" src/components/ui/BlinkingCursor.tsx (reduced motion support)
</acceptance_criteria>
</task>

<task id="03-02-T5" title="Create TerminalPrompt component" depends_on="03-02-T4">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (TerminalPrompt: Component API -- renders mattia@selfrules ~ $ + BlinkingCursor block)
- src/components/ui/BlinkingCursor.tsx (verify BlinkingCursor is available)
</read_first>

<actions>
1. Create `src/components/ui/TerminalPrompt.tsx`:
   - Import `BlinkingCursor` from `@/components/ui/BlinkingCursor`
   - Import `cn` from `@/lib/utils`
   - Interface `TerminalPromptProps`: `className?: string`
   - Export named function `TerminalPrompt`
   - Render: `<div>` wrapper with `flex items-center gap-2`
   - Text: `<span className="font-mono text-[13px] text-tertiary">mattia@selfrules ~ $</span>`
   - Cursor: `<BlinkingCursor type="block" />`
   - NO `'use client'` (Server Component that composes a Client Component)
</actions>

<acceptance_criteria>
- grep -q "export function TerminalPrompt" src/components/ui/TerminalPrompt.tsx
- grep -q "import.*BlinkingCursor" src/components/ui/TerminalPrompt.tsx
- grep -q "mattia@selfrules" src/components/ui/TerminalPrompt.tsx
- grep -q "font-mono" src/components/ui/TerminalPrompt.tsx
- grep -q "text-tertiary" src/components/ui/TerminalPrompt.tsx
- grep -q 'type="block"' src/components/ui/TerminalPrompt.tsx
- grep -qv "use client" src/components/ui/TerminalPrompt.tsx
</acceptance_criteria>
</task>

<task id="03-02-T6" title="Create CountUpNumber client component">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (CountUpNumber: Component API, behavior specs, reduced motion)
- .planning/phases/03-ui-primitives/03-RESEARCH.md (discretion recommendation: clamp() for number sizing)
</read_first>

<actions>
1. Create `src/components/ui/CountUpNumber.tsx`:
   - Add `'use client'` directive
   - Import `useRef`, `useState`, `useEffect` from `react`
   - Import `cn` from `@/lib/utils`
   - Interface `CountUpNumberProps`: `value: string`, `className?: string`
   - Export named function `CountUpNumber`
   - Parse `value` to extract:
     - `prefix`: leading non-numeric chars (e.g., "-" from "-25%")
     - `numericPart`: the number itself (e.g., 25 from "-25%")
     - `suffix`: trailing non-numeric chars (e.g., "%", "%+", "+")
   - Use `useRef` for the element reference and `IntersectionObserver`
   - Trigger at `threshold: 0.3`
   - Animation: `requestAnimationFrame` loop, 800ms duration, ease-out easing
   - Animate from 0 to `numericPart`, displaying `prefix + currentValue + suffix`
   - Reduced motion: check `window.matchMedia('(prefers-reduced-motion: reduce)')`. If true, render final value immediately with no animation.
   - Display the value in a `<span>` -- the parent component handles font/color styling via className
   - Disconnect observer after animation completes (run once)
   - ~30 lines of implementation. NO animation library.
</actions>

<acceptance_criteria>
- grep -q "use client" src/components/ui/CountUpNumber.tsx
- grep -q "export function CountUpNumber" src/components/ui/CountUpNumber.tsx
- grep -q "IntersectionObserver" src/components/ui/CountUpNumber.tsx
- grep -q "requestAnimationFrame" src/components/ui/CountUpNumber.tsx
- grep -q "threshold" src/components/ui/CountUpNumber.tsx
- grep -q "800\|duration" src/components/ui/CountUpNumber.tsx
- grep -q "reduced-motion\|prefers-reduced-motion" src/components/ui/CountUpNumber.tsx
- grep -q "prefix\|suffix" src/components/ui/CountUpNumber.tsx
- grep -q "useRef\|useState\|useEffect" src/components/ui/CountUpNumber.tsx
</acceptance_criteria>
</task>

<task id="03-02-T7" title="Create VerticalTimeline container component">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (VerticalTimeline: Layout Contracts, vertical line specs)
</read_first>

<actions>
1. Create `src/components/ui/VerticalTimeline.tsx`:
   - Import `cn` from `@/lib/utils`
   - Interface `VerticalTimelineProps`: `children: React.ReactNode`, `className?: string`
   - Export named function `VerticalTimeline`
   - Outer `<div>` with `relative` positioning and left padding for line + nodes: `pl-10 md:pl-12`
   - Vertical line: `<div>` with absolute positioning:
     `absolute left-[14px] md:left-[16px] top-0 bottom-0 w-[2px] bg-[var(--border-default)]`
   - Children rendered inside the container (TimelineNode components)
   - NO `'use client'`
</actions>

<acceptance_criteria>
- grep -q "export function VerticalTimeline" src/components/ui/VerticalTimeline.tsx
- grep -q "relative" src/components/ui/VerticalTimeline.tsx
- grep -q "absolute" src/components/ui/VerticalTimeline.tsx
- grep -q "w-\[2px\]" src/components/ui/VerticalTimeline.tsx
- grep -q "border-default" src/components/ui/VerticalTimeline.tsx
- grep -q "children" src/components/ui/VerticalTimeline.tsx
- grep -qv "use client" src/components/ui/VerticalTimeline.tsx
</acceptance_criteria>
</task>

<task id="03-02-T8" title="Create TimelineNode component">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (TimelineNode: Component API, Layout Contracts, square nodes, connector, Interaction Contracts hover)
- .planning/phases/03-ui-primitives/03-CONTEXT.md (D-04: TimelineBlock hover border-accent/50 decided)
- DESIGN-UPDATE-v25.md (point 7: square nodes 0px border-radius, last node amber filled)
</read_first>

<actions>
1. Create `src/components/ui/TimelineNode.tsx`:
   - Import `cn` from `@/lib/utils`
   - Interface `TimelineNodeProps`: `role: string`, `company: string`, `dates: string`, `description?: string`, `isActive?: boolean` (default false), `className?: string`
   - Export named function `TimelineNode`
   - Outer `<div>` with `relative mb-8` (gap between nodes)
   - Node square: `<div>` with absolute positioning aligned to vertical line center:
     `absolute -left-[calc(theme(spacing.10)-7px)] md:-left-[calc(theme(spacing.12)-7px)]`
     Size: `w-[10px] h-[10px]`
     Active: `bg-accent` (filled amber)
     Inactive: `border border-default bg-transparent`
     Border-radius: 0 (square -- consistent with site identity, per DESIGN-UPDATE-v25)
   - Horizontal connector: `<div>` absolute from node right edge to content block:
     `absolute top-[4px] -left-[calc(theme(spacing.10)-17px)] md:-left-[calc(theme(spacing.12)-17px)] w-[16px] md:w-[20px] h-[1px] bg-[var(--border-default)]`
   - Content block: `<div>` with:
     `border border-default bg-surface p-6 transition-[border-color] duration-200 hover:border-accent/50`
   - Inside content block:
     - Dates: `<p>` with `font-mono text-[13px] text-tertiary`
     - Role: `<h3>` with `font-heading font-bold text-xl text-primary mt-1`
     - Company: `<p>` with `text-base text-secondary mt-1`
     - Description (optional): `<p>` with `text-base text-secondary mt-2`
   - NO `'use client'`
   - NO rounded corners on node (0px border-radius)
</actions>

<acceptance_criteria>
- grep -q "export function TimelineNode" src/components/ui/TimelineNode.tsx
- grep -q "isActive" src/components/ui/TimelineNode.tsx
- grep -q "bg-accent" src/components/ui/TimelineNode.tsx
- grep -q "w-\[10px\]" src/components/ui/TimelineNode.tsx
- grep -q "bg-surface" src/components/ui/TimelineNode.tsx
- grep -q "p-6" src/components/ui/TimelineNode.tsx
- grep -q "hover:border-accent/50" src/components/ui/TimelineNode.tsx
- grep -q "font-mono" src/components/ui/TimelineNode.tsx
- grep -q "text-tertiary" src/components/ui/TimelineNode.tsx
- grep -q "font-heading" src/components/ui/TimelineNode.tsx
- grep -q "h-\[1px\]" src/components/ui/TimelineNode.tsx (horizontal connector)
- grep -qv "use client" src/components/ui/TimelineNode.tsx
- grep -qv "rounded" src/components/ui/TimelineNode.tsx (no rounded on node -- must be square)
</acceptance_criteria>
</task>

<task id="03-02-T9" title="Create /dev/components verification route" depends_on="03-02-T1,03-02-T2,03-02-T3,03-02-T4,03-02-T5,03-02-T6,03-02-T7,03-02-T8">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (Dev Verification Route section: layout, sample data for each component, Copywriting Contract sample props)
- .planning/phases/03-ui-primitives/03-RESEARCH.md (Pattern 6: Dev-Only Route with notFound())
- All component files created in previous tasks (verify exports)
</read_first>

<actions>
1. Create `src/app/[locale]/dev/components/page.tsx`:
   - Import `notFound` from `next/navigation`
   - Production guard at top of function: `if (process.env.NODE_ENV === 'production') notFound()`
   - Import ALL Phase 3 components: Button, Tag, Section, SectionHeader, MetricCard, CaseStudyCard, ProjectCard, BlinkingCursor, TerminalPrompt, CountUpNumber, VerticalTimeline, TimelineNode
   - Page layout: `<main className="py-20 px-8 max-w-[1080px] mx-auto">`
   - Sections separated by `<hr className="border-default my-16" />`
   - Each section has an h2 title and the component(s) with sample data from UI-SPEC Copywriting Contract:

   **Section: Button**
   - Show primary and secondary side by side: "Get in touch ->" primary, "See the work ->" secondary

   **Section: Tag**
   - Show bordered (default): "B2B SaaS", "Payments", "10+ years"
   - Show borderless: same text with `bordered={false}`

   **Section: SectionHeader**
   - Show: label="NUMBERS, NOT WORDS", title="Numeri, non parole"

   **Section: Section**
   - Show default width (720px) with placeholder content
   - Show wide variant (1080px) with placeholder content

   **Section: MetricCard Grid**
   - Show 6 cards in the gap-[1px] grid container:
     `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-[1px] bg-[var(--border-default)] border border-default">`
   - Sample data: "99%+"/Uptime/Payment success rate, "-25%"/Incidents/Post-release (QubicaAMF), "116"/Markets/Countries with active deployments, "3"/Roles/Designer Developer PM, "10+"/Years/Cross-functional product work, "$2M+"/Revenue/Annual recurring influenced

   **Section: CaseStudyCard**
   - Show 2 cards stacked: one with tag/title/preview/metric/metricLabel, one simpler

   **Section: ProjectCard**
   - Show: CasaHunter card with active status, plus a shipped card example

   **Section: BlinkingCursor**
   - Show both block and underscore variants

   **Section: TerminalPrompt**
   - Show the full prompt line

   **Section: CountUpNumber**
   - Show 3 variants: "-25%", "116", "99%+"

   **Section: VerticalTimeline + TimelineNode**
   - Show 3 nodes: Designer (inactive), Frontend Developer (inactive), Product Manager (active with description)

2. NO `generateStaticParams` needed (dev-only, not statically rendered).
3. NO `setRequestLocale` needed (dev-only).
</actions>

<acceptance_criteria>
- grep -q "notFound" src/app/\[locale\]/dev/components/page.tsx
- grep -q "NODE_ENV.*production" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*Button" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*Tag" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*MetricCard" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*CaseStudyCard" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*ProjectCard" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*BlinkingCursor" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*TerminalPrompt" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*CountUpNumber" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*VerticalTimeline" src/app/\[locale\]/dev/components/page.tsx
- grep -q "import.*TimelineNode" src/app/\[locale\]/dev/components/page.tsx
- grep -q "gap-\[1px\]" src/app/\[locale\]/dev/components/page.tsx
- grep -q "99%+" src/app/\[locale\]/dev/components/page.tsx
</acceptance_criteria>
</task>

<task id="03-02-T10" title="Build verification and visual checkpoint" depends_on="03-02-T9">
<read_first>
- src/app/[locale]/dev/components/page.tsx (the dev route just created)
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (Phase 3 Success Criteria Visual: 32 criteria)
</read_first>

<actions>
1. Run `npx tsc --noEmit` to verify no TypeScript errors.
2. Run `npm run build` to verify no build-time errors.
3. If build errors, fix them in the relevant files.
4. Verify production exclusion: the /dev/components route should NOT appear in the build output as a static page (it calls notFound() in production).
5. Document any issues found and their resolutions.
6. Create a summary of all 12 components created in Phase 3 with their file paths.
</actions>

<acceptance_criteria>
- npx tsc --noEmit exits with code 0
- npm run build completes without errors
- All 12 component files exist under src/components/
- The dev route file exists at src/app/[locale]/dev/components/page.tsx
- No TypeScript errors remain
</acceptance_criteria>
</task>
