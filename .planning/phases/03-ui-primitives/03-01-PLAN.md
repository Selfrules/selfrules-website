---
phase: 03-ui-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils.ts
  - src/components/ui/Tag.tsx
  - src/components/ui/Button.tsx
  - src/components/layout/Section.tsx
  - src/components/layout/SectionHeader.tsx
  - package.json
autonomous: true
requirements: [UICM-01, UICM-06, UICM-07]

must_haves:
  truths:
    - "cn() utility is available at src/lib/utils.ts and handles conditional class merging"
    - "Tag renders in JetBrains Mono, 13px, uppercase, tracking 0.05em, with bordered and borderless variants"
    - "Button renders as Link when href provided, button when absent, with primary and secondary variants"
    - "Button primary has bg-accent, text-dark, hover bg-accent-hover + translate-y(-1px)"
    - "Button secondary has transparent bg, border-default, hover border-accent + text-accent"
    - "Section wrapper constrains content to 720px default or 1080px wide max-width"
    - "Section wrapper applies section-gap vertical padding and page-padding horizontal padding"
    - "SectionHeader renders monospace uppercase label + Space Grotesk 700 title"
    - "All components have 0px border-radius"
    - "No component uses 'use client' directive"
  artifacts:
    - path: "src/lib/utils.ts"
      provides: "cn() class name merging utility"
      exports: ["cn"]
    - path: "src/components/ui/Tag.tsx"
      provides: "Monospace uppercase label component with optional border"
      exports: ["Tag"]
    - path: "src/components/ui/Button.tsx"
      provides: "Polymorphic CTA button (Link or button element) with primary/secondary variants"
      exports: ["Button"]
    - path: "src/components/layout/Section.tsx"
      provides: "Layout wrapper with section-gap, max-width, page-padding"
      exports: ["Section"]
    - path: "src/components/layout/SectionHeader.tsx"
      provides: "Label (monospace) + title (Space Grotesk) pattern"
      exports: ["SectionHeader"]
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/lib/utils.ts"
      via: "import cn"
      pattern: "import.*cn.*from.*lib/utils"
    - from: "src/components/ui/Button.tsx"
      to: "next/link"
      via: "import Link"
      pattern: "import.*Link.*from.*next/link"
    - from: "src/components/ui/Tag.tsx"
      to: "src/lib/utils.ts"
      via: "import cn"
      pattern: "import.*cn.*from.*lib/utils"
    - from: "src/components/layout/Section.tsx"
      to: "src/lib/utils.ts"
      via: "import cn"
      pattern: "import.*cn.*from.*lib/utils"
    - from: "src/components/layout/SectionHeader.tsx"
      to: "src/lib/utils.ts"
      via: "import cn"
      pattern: "import.*cn.*from.*lib/utils"
---

<objective>
Create the foundational UI utility and components: cn() helper, Tag, Button, Section wrapper, and SectionHeader. These are leaf-level building blocks with no dependencies on other Phase 3 components. Every subsequent component (MetricCard, CaseStudyCard, etc.) depends on Tag and/or Section.

Purpose: Establish the component patterns (Server Components, typed props, cn() for class merging, Tailwind v4 tokens) that all other Phase 3 components follow.
Output: 5 importable, typed components with correct styling, hover states (CSS-only), and 0px border-radius.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-primitives/03-CONTEXT.md
@.planning/phases/03-ui-primitives/03-RESEARCH.md
@.planning/phases/03-ui-primitives/03-UI-SPEC.md
@DESIGN-UPDATE-v25.md
</context>

<task id="03-01-T1" title="Install clsx and create cn() utility">
<read_first>
- src/lib/utils.ts (check if exists from Phase 1)
- package.json (check if clsx already installed)
- src/app/globals.css (verify @theme tokens are available: --border-default, --bg-primary, --bg-surface, --section-gap, --content-width, --wide-width, --page-padding)
</read_first>

<actions>
1. Check if `clsx` is in package.json dependencies. If not, run `npm install clsx`.
2. Create `src/lib/utils.ts` with:
   - Import `clsx` and its `ClassValue` type
   - Export `cn(...inputs: ClassValue[]): string` that returns `clsx(inputs)`
   - No tailwind-merge needed (project has no conflicting utility classes)
3. Verify the file compiles: `npx tsc --noEmit src/lib/utils.ts` (or just check no red squiggles).
</actions>

<acceptance_criteria>
- grep -q "export function cn" src/lib/utils.ts
- grep -q "clsx" src/lib/utils.ts
- grep -q "clsx" package.json
</acceptance_criteria>
</task>

<task id="03-01-T2" title="Create Tag component" depends_on="03-01-T1">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (Tag section: Component API Contracts, Typography table for Small/13px, Color table for text-secondary)
- src/app/globals.css (verify font-mono maps to JetBrains Mono via --font-mono CSS variable)
</read_first>

<actions>
1. Create `src/components/ui/Tag.tsx`:
   - Interface `TagProps`: `children: React.ReactNode`, `bordered?: boolean` (default true), `className?: string`
   - Export named function `Tag`
   - Base classes: `font-mono text-[13px] uppercase tracking-[0.05em] text-secondary inline-block`
   - Bordered variant adds: `px-3 py-1 bg-surface border border-default`
   - Use `cn()` for conditional class merging
   - NO `'use client'` directive
   - NO border-radius (0px is global default)
2. Verify TypeScript compiles without errors.
</actions>

<acceptance_criteria>
- grep -q "export function Tag" src/components/ui/Tag.tsx
- grep -q "font-mono" src/components/ui/Tag.tsx
- grep -q "uppercase" src/components/ui/Tag.tsx
- grep -q "tracking-\[0.05em\]" src/components/ui/Tag.tsx
- grep -q "text-secondary" src/components/ui/Tag.tsx
- grep -q "bordered" src/components/ui/Tag.tsx
- grep -qv "use client" src/components/ui/Tag.tsx (must NOT contain 'use client')
</acceptance_criteria>
</task>

<task id="03-01-T3" title="Create Button component" depends_on="03-01-T1">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (Button section: Interaction Contracts, Component API Contracts)
- .planning/phases/03-ui-primitives/03-RESEARCH.md (Pattern 2: Polymorphic Button)
</read_first>

<actions>
1. Create `src/components/ui/Button.tsx`:
   - Import `Link` from `next/link` and `cn` from `@/lib/utils`
   - Interface `ButtonProps`:
     - `variant: 'primary' | 'secondary'` (required)
     - `children: React.ReactNode` (required)
     - `href?: string` (optional -- determines element type)
     - `className?: string` (optional)
   - Shared base classes: `inline-flex items-center justify-center h-12 px-8 text-base font-normal transition-all duration-150`
   - Primary variant: `bg-accent text-dark hover:bg-accent-hover hover:-translate-y-[1px]`
   - Secondary variant: `bg-transparent border border-default text-primary hover:border-accent hover:text-accent`
   - When `href` provided: render `<Link href={href} className={classes}>{children}</Link>`
   - When no `href`: render `<button type="button" className={classes}>{children}</button>`
   - Add focus styles: `focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2`
   - NO `'use client'` directive (Link works in Server Components)
   - NO border-radius (0px is global)
2. Verify TypeScript compiles.
</actions>

<acceptance_criteria>
- grep -q "export function Button" src/components/ui/Button.tsx
- grep -q "variant.*primary.*secondary" src/components/ui/Button.tsx
- grep -q "bg-accent" src/components/ui/Button.tsx
- grep -q "text-dark" src/components/ui/Button.tsx
- grep -q "hover:bg-accent-hover" src/components/ui/Button.tsx
- grep -q "hover:-translate-y" src/components/ui/Button.tsx
- grep -q "border-default" src/components/ui/Button.tsx
- grep -q "hover:border-accent" src/components/ui/Button.tsx
- grep -q "hover:text-accent" src/components/ui/Button.tsx
- grep -q "next/link" src/components/ui/Button.tsx
- grep -q "h-12" src/components/ui/Button.tsx
- grep -q "px-8" src/components/ui/Button.tsx
- grep -q 'type="button"' src/components/ui/Button.tsx
- grep -q "focus-visible" src/components/ui/Button.tsx
- grep -qv "use client" src/components/ui/Button.tsx
</acceptance_criteria>
</task>

<task id="03-01-T4" title="Create Section layout wrapper" depends_on="03-01-T1">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (Section Wrapper layout contract: section-gap, content-width, wide-width, page-padding)
- src/app/globals.css (verify CSS custom properties: --section-gap, --content-width, --wide-width, --page-padding exist in @theme)
</read_first>

<actions>
1. Create `src/components/layout/Section.tsx`:
   - Interface `SectionProps`:
     - `children: React.ReactNode` (required)
     - `wide?: boolean` (default false -- 720px; true = 1080px)
     - `className?: string` (optional)
     - `id?: string` (optional, for anchor linking)
   - Outer element: `<section>` with `id` prop
   - Outer padding: `py-[var(--section-gap)] px-[var(--page-padding)]`
   - Inner `<div>`: `mx-auto w-full`
   - Default max-width: `max-w-[var(--content-width)]` (720px)
   - Wide max-width: `max-w-[var(--wide-width)]` (1080px)
   - Use `cn()` for conditional class merging
   - NO `'use client'` directive
2. Verify TypeScript compiles.
</actions>

<acceptance_criteria>
- grep -q "export function Section" src/components/layout/Section.tsx
- grep -q "section-gap" src/components/layout/Section.tsx
- grep -q "page-padding" src/components/layout/Section.tsx
- grep -q "content-width" src/components/layout/Section.tsx
- grep -q "wide-width" src/components/layout/Section.tsx
- grep -q "<section" src/components/layout/Section.tsx
- grep -q "mx-auto" src/components/layout/Section.tsx
- grep -q "wide" src/components/layout/Section.tsx
- grep -qv "use client" src/components/layout/Section.tsx
</acceptance_criteria>
</task>

<task id="03-01-T5" title="Create SectionHeader component" depends_on="03-01-T1">
<read_first>
- .planning/phases/03-ui-primitives/03-UI-SPEC.md (SectionHeader API: label monospace + title Space Grotesk 700, Typography table)
- src/app/globals.css (verify font-heading maps to Space Grotesk via --font-heading CSS variable)
</read_first>

<actions>
1. Create `src/components/layout/SectionHeader.tsx`:
   - Interface `SectionHeaderProps`:
     - `label: string` (required -- monospace uppercase, e.g., "HOW I WORK")
     - `title: string` (required -- section title in Space Grotesk 700)
     - `className?: string` (optional)
   - Layout: vertical stack with `mb-12` bottom margin
   - Label: `<p>` with `font-mono text-[13px] uppercase tracking-[0.05em] text-secondary mb-2`
   - Title: `<h2>` with `text-xl font-bold text-primary font-heading`
   - Use `cn()` for className merging
   - NO `'use client'` directive
2. Verify TypeScript compiles.
</actions>

<acceptance_criteria>
- grep -q "export function SectionHeader" src/components/layout/SectionHeader.tsx
- grep -q "font-mono" src/components/layout/SectionHeader.tsx
- grep -q "uppercase" src/components/layout/SectionHeader.tsx
- grep -q "tracking-\[0.05em\]" src/components/layout/SectionHeader.tsx
- grep -q "text-secondary" src/components/layout/SectionHeader.tsx
- grep -q "font-heading" src/components/layout/SectionHeader.tsx
- grep -q "font-bold" src/components/layout/SectionHeader.tsx
- grep -q "<h2" src/components/layout/SectionHeader.tsx
- grep -q "mb-12" src/components/layout/SectionHeader.tsx
- grep -qv "use client" src/components/layout/SectionHeader.tsx
</acceptance_criteria>
</task>

<task id="03-01-T6" title="Verify all foundational components build" depends_on="03-01-T2,03-01-T3,03-01-T4,03-01-T5">
<read_first>
- src/components/ui/Tag.tsx
- src/components/ui/Button.tsx
- src/components/layout/Section.tsx
- src/components/layout/SectionHeader.tsx
- src/lib/utils.ts
</read_first>

<actions>
1. Run `npx tsc --noEmit` to verify no TypeScript errors across the project.
2. Run `npm run build` to verify no build-time errors.
3. If errors exist, fix them in the relevant component files.
4. Verify all 5 files exist and export their named components.
</actions>

<acceptance_criteria>
- npx tsc --noEmit exits with code 0
- All 5 files exist: src/lib/utils.ts, src/components/ui/Tag.tsx, src/components/ui/Button.tsx, src/components/layout/Section.tsx, src/components/layout/SectionHeader.tsx
- Each file exports its named function (verified by grep in previous tasks)
</acceptance_criteria>
</task>
