---
phase: 3
slug: ui-primitives
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-22
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the UI Primitives phase: Button, Tag, Section, MetricCard, CaseStudyCard, ProjectCard, TimelineBlock, and /dev/components verification route. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (pure Tailwind v4 utilities) |
| Icon library | none (arrow in CTA buttons is a text character or inline SVG) |
| Font | Inter (variable, 400-700) + JetBrains Mono (variable, 400-700) via next/font/google |

**Inherited from Phase 1.** No component library, no shadcn. All styling via Tailwind CSS v4 `@theme` tokens in globals.css. Locked project-level decision.

---

## Spacing Scale

Inherited from Phase 1 UI-SPEC. All values are multiples of 4.

| Token | Value | Usage in Phase 3 |
|-------|-------|-------------------|
| xs | 4px | Tag inline padding (py), icon-to-text gap in Button |
| sm | 8px | Tag horizontal padding (px), gap between credential tags |
| md | 16px | MetricCard internal gap between number/label/context, ProjectCard internal gaps |
| lg | 24px | Card internal padding mobile (minimum) |
| xl | 32px | MetricCard padding, CaseStudyCard padding mobile (p-8), ProjectCard padding |
| 2xl | 48px | CaseStudyCard padding desktop (md:p-12) |
| 3xl | 64px | Not used directly in component internals |

**Phase-specific spacing (inherited from Phase 1):**

| Token | Value | Usage in Phase 3 |
|-------|-------|-------------------|
| section-gap | `clamp(80px, 10vw, 160px)` | Section wrapper vertical padding (py) |
| content-width | 720px | Section wrapper default max-width |
| wide-width | 1080px | Section wrapper `wide` variant max-width |
| page-padding | `clamp(20px, 5vw, 80px)` | Section wrapper horizontal padding (px) |

**Exceptions:**
- Button height: 48px (h-12) -- standard touch target, not a spacing token
- MetricCard number font size uses `clamp()` which may produce non-4px-multiple values at intermediate viewport widths. This is acceptable for fluid typography.

---

## Typography

Inherited from Phase 1. Phase 3 components use exactly 4 sizes and 2 weights.

| Role | Size | Weight | Line Height | Font | Phase 3 Usage |
|------|------|--------|-------------|------|---------------|
| Small | 13px | 400 | 1.4 | Inter or JetBrains Mono | Tag text (mono, uppercase, tracking 0.05em), MetricCard context, section label (mono, uppercase, tracking 0.05em), MetricCard label, ProjectCard status label, TimelineBlock dates (mono), CaseStudyCard tag (via Tag component) |
| Body | 16px | 400 | 1.7 | Inter | Button text, CaseStudyCard preview, ProjectCard one-liner, TimelineBlock company, TimelineBlock description |
| Title | 20px | 700 | 1.3 | Inter | CaseStudyCard title, ProjectCard title, TimelineBlock role, SectionHeader title |
| Metric | `clamp(40px, 5vw, 72px)` | 700 | 1.0 | JetBrains Mono | MetricCard number (accent color), CaseStudyCard metric number (accent color, right-aligned) |

**Weight map (2 weights only):**

| Weight | Value | Usage |
|--------|-------|-------|
| Regular | 400 | Body text, button labels, small/context text, tags, section labels, metric labels, status labels, dates |
| Bold | 700 | Card titles, section titles, metric display numbers |

**Phase 3 specific type rules:**

| Element | Size | Weight | Font | Details |
|---------|------|--------|------|---------|
| Button text | 16px (Body) | 400 | Inter | Normal case |
| Tag text | 13px (Small) | 400 | JetBrains Mono | `text-transform: uppercase`, `letter-spacing: 0.05em` |
| Section label | 13px (Small) | 400 | JetBrains Mono | `text-transform: uppercase`, `letter-spacing: 0.05em`, text-secondary |
| MetricCard number | clamp(40px, 5vw, 72px) (Metric) | 700 | JetBrains Mono | Accent color (#E8A838) |
| MetricCard label | 13px (Small) | 400 | Inter | text-primary |
| MetricCard context | 13px (Small) | 400 | Inter | text-tertiary |
| CaseStudyCard tag | 13px (Small) | 400 | JetBrains Mono | Uppercase, tracking-wide, text-secondary (uses Tag component) |
| CaseStudyCard title | 20px (Title) | 700 | Inter | text-primary |
| CaseStudyCard preview | 16px (Body) | 400 | Inter | text-secondary |
| CaseStudyCard metric | clamp(40px, 5vw, 72px) (Metric) | 700 | JetBrains Mono | Accent color, right-aligned |
| ProjectCard title | 20px (Title) | 700 | Inter | text-primary |
| ProjectCard one-liner | 16px (Body) | 400 | Inter | text-secondary |
| ProjectCard status label | 13px (Small) | 400 | Inter | text-secondary |
| TimelineBlock dates | 13px (Small) | 400 | JetBrains Mono | text-tertiary |
| TimelineBlock role | 20px (Title) | 700 | Inter | text-primary |
| TimelineBlock company | 16px (Body) | 400 | Inter | text-secondary |

---

## Color

**60/30/10 split:** 60% dominant background (#0A0A0B), 30% surface/cards/borders (#111113 + #1A1A1F), 10% accent (#E8A838) reserved for CTAs, metric numbers, and hover highlights.

Inherited from Phase 1. Phase 3 uses all tokens:

| Role | Value | Tailwind Token | Phase 3 Usage |
|------|-------|----------------|---------------|
| Dominant (60%) | `#0A0A0B` | `bg-primary` | MetricCard cell background, page background behind components |
| Surface | `#111113` | `bg-surface` | Tag background (optional border variant), TimelineBlock card background |
| Hover surface | `#1A1A1F` | `bg-hover` | Not used directly (hover is expressed through border color, not bg) |
| Border default | `#1A1A1F` | `border-default` | All card borders (1px), MetricCard grid gap color, Tag border |
| Text primary | `#F5F5F0` | `text-primary` | Card titles, metric labels, button secondary text |
| Text secondary | `#8A8A8E` | `text-secondary` | Card descriptions, tags, section labels, button secondary hover text |
| Text tertiary | `#5A5A5E` | `text-tertiary` | MetricCard context, TimelineBlock dates |
| Accent (10%) | `#E8A838` | `accent` | See reserved-for list below |
| Accent hover | `#D4962F` | `accent-hover` | Button primary hover background |
| Border accent | `#E8A838` | `border-accent` | Card hover border color (at varying opacity) |
| Dark text | `#0A0A0B` | `text-dark` | Button primary text (dark on accent bg) |

**Accent reserved for (Phase 3 scope):**
1. Button primary -- background color
2. MetricCard number -- text color
3. CaseStudyCard metric number -- text color (right side)
4. Card hover states -- border-accent/40 (40% opacity) on MetricCard, CaseStudyCard, ProjectCard, TimelineBlock
5. Section label -- NOT accent (it is text-secondary; accent is reserved for interactive/data elements)

**No destructive color needed in Phase 3.** No destructive actions exist in the component library.

---

## Border Radius

Inherited from Phase 1. Global rule: **0px on every element.** No exceptions in Phase 3. Applies to: Button, Tag, MetricCard, CaseStudyCard, ProjectCard, TimelineBlock, Section wrapper.

---

## Interaction Contracts

### Button Hover (UICM-01)

| Variant | Default State | Hover State | Transition |
|---------|---------------|-------------|------------|
| Primary | `bg-accent`, `text-dark` (#0A0A0B) | `bg-accent-hover`, `translate-y(-1px)` | `transition: all 150ms ease` |
| Secondary | `bg-transparent`, `border border-default`, `text-primary` | `border-accent`, `text-accent` | `transition: all 150ms ease` |

Both variants:
- Height: 48px (h-12)
- Padding: 12px vertical, 24px horizontal (inferred from `px-8` = 32px in Figma prototype -- use `px-8`)
- Corrected padding from prototype: `h-12 px-8` (48px height, 32px horizontal padding)
- Font weight: 400
- Display: `inline-flex items-center justify-center`
- Border radius: 0px
- When `href` is provided: renders as `<a>` (or Next.js `<Link>`). When absent: renders as `<button>`.
- Arrow suffix (" ->") is part of the label string passed via props, not generated by the component

### MetricCard Hover (UICM-02)

| Property | Default | Hover | Transition |
|----------|---------|-------|------------|
| Border color | `border-default` (#1A1A1F) | `border-accent/40` (#E8A838 at 40% opacity) | `transition: border-color 200ms ease` |

MetricCard does NOT have translate-y hover. Only border color changes.

### CaseStudyCard Hover (UICM-03)

| Property | Default | Hover | Transition |
|----------|---------|-------|------------|
| Border color | `border-default` (#1A1A1F) | `border-accent/40` (#E8A838 at 40% opacity) | `transition: all 200ms ease` |
| Transform | none | `translate-y(-2px)` | included in `all 200ms` |

### ProjectCard Hover (UICM-04)

| Property | Default | Hover | Transition |
|----------|---------|-------|------------|
| Border color | `border-default` (#1A1A1F) | `border-accent/40` (#E8A838 at 40% opacity) | `transition: border-color 200ms ease` |

ProjectCard does NOT have translate-y hover. Only border color changes.

### TimelineBlock Hover

| Property | Default | Hover | Transition |
|----------|---------|-------|------------|
| Border color | `border-default` (#1A1A1F) | `border-accent/50` (#E8A838 at 50% opacity) | `transition: border-color 200ms ease` |

Per Claude's discretion area in CONTEXT.md: TimelineBlock gets hover border-accent/50 for consistency with other card types. No translate-y.

---

## Component Inventory

Phase 3 introduces the following components:

| Component | File Path | Server/Client | Purpose |
|-----------|-----------|---------------|---------|
| `Button` | `src/components/ui/Button.tsx` | Server | Primary and secondary CTA buttons, renders as `<a>`/`<Link>` or `<button>` |
| `Tag` | `src/components/ui/Tag.tsx` | Server | Monospace uppercase credential/tech labels |
| `Section` | `src/components/layout/Section.tsx` | Server | Layout wrapper with section-gap, max-width, optional SectionHeader |
| `SectionHeader` | `src/components/layout/SectionHeader.tsx` | Server | Label (monospace) + title pattern used by most homepage sections |
| `MetricCard` | `src/components/ui/MetricCard.tsx` | Server | Accent number + label + context for results grid |
| `CaseStudyCard` | `src/components/ui/CaseStudyCard.tsx` | Server | Flex layout card for work page (tag, title, preview, metric) |
| `ProjectCard` | `src/components/ui/ProjectCard.tsx` | Server | Side project card (title, one-liner, tech tags, status) |
| `TimelineBlock` | `src/components/ui/TimelineBlock.tsx` | Server | Career timeline card (role, company, dates, description) |

All components are Server Components (no interactivity beyond CSS hover states). No useState, no useEffect, no event handlers. CSS handles all hover transitions.

### Component API Contracts

**Button:**
```
Props:
  variant: 'primary' | 'secondary'    -- required
  children: React.ReactNode            -- required (label text including arrow)
  href?: string                        -- if provided, renders Link/a; if absent, renders button
  className?: string                   -- optional override
  ...rest: ButtonHTMLAttributes | AnchorHTMLAttributes
```

**Tag:**
```
Props:
  children: React.ReactNode            -- required (label text)
  bordered?: boolean                   -- default true (1px border + surface bg); false = no border, no bg
  className?: string
```

**Section:**
```
Props:
  children: React.ReactNode            -- required
  wide?: boolean                       -- default false (720px); true = 1080px max-width
  className?: string
  id?: string                          -- for anchor linking (e.g., id="contact")
```

**SectionHeader:**
```
Props:
  label: string                        -- monospace uppercase label (e.g., "HOW I WORK")
  title: string                        -- section title
  className?: string
```

**MetricCard:**
```
Props:
  number: string                       -- e.g., "99%+", "-25%", "116"
  label: string                        -- metric name
  context: string                      -- one-line explanation
  className?: string
```

**CaseStudyCard:**
```
Props:
  tag: string                          -- e.g., "B2B SaaS" (rendered via Tag component)
  title: string                        -- result-led title
  preview: string                      -- 1-2 sentence description
  metric: string                       -- large accent number (e.g., "-25%")
  metricLabel?: string                 -- short label under metric (e.g., "post-release incidents")
  className?: string
```

**ProjectCard:**
```
Props:
  title: string
  description: string                  -- one-liner
  techStack: string[]                  -- rendered as Tag components
  status: 'active' | 'shipped' | 'coming-soon'
  statusLabel: string                  -- display text for status (i18n)
  href?: string                        -- optional link to repo/demo
  className?: string
```

**TimelineBlock:**
```
Props:
  role: string                         -- job title
  company: string                      -- company/context
  dates: string                        -- e.g., "2023 - oggi"
  description: string                  -- 1-2 sentences
  className?: string
```

---

## Layout Contracts

### MetricCard Grid (UICM-02, D-03)

The MetricCard grid uses the "shared border" pattern from the Figma prototype:

```
Container:
  display: grid
  grid-template-columns: repeat(3, 1fr)  -- desktop
  grid-template-columns: repeat(2, 1fr)  -- tablet (md breakpoint)
  grid-template-columns: repeat(1, 1fr)  -- mobile (default)
  gap: 1px
  background: var(--border-default)       -- the gap IS the border
  border: 1px solid var(--border-default)

Each MetricCard cell:
  background: var(--bg-primary)           -- fills the cell, gap shows through as border
  padding: 32px
```

This creates the effect of shared 1px borders between cells (like a table grid). The container background color bleeds through the 1px gap.

### CaseStudyCard Layout (UICM-03, D-02)

```
Card:
  display: flex
  flex-direction: row (desktop) / column (mobile)
  border: 1px solid var(--border-default)
  padding: 32px mobile (p-8) / 48px desktop (md:p-12)
  gap: 24px (between left content and right metric)

Left side (flex: 1):
  Tag (top)
  Title (below tag, mt-3)
  Preview text (below title, mt-2)

Right side (flex-shrink: 0, self-center):
  Large accent number (clamp 40-72px, font-mono, font-bold)
  Optional metric label below (13px, text-tertiary)
  Arrow icon (inline SVG, 16px, accent color, to the right of number)
```

On mobile (<768px), the right-side metric moves below the preview text (flex-direction: column).

### ProjectCard Layout (UICM-04)

```
Card:
  border: 1px solid var(--border-default)
  padding: 32px (p-8)

Content stack (vertical):
  Row 1: Title + status dot + status label (inline)
  Row 2: One-liner description (mt-2)
  Row 3: Tech stack tags (mt-4, flex wrap, gap-2)
  Row 4: Optional link (mt-4, accent color, arrow suffix)

Status dot:
  width: 8px, height: 8px, border-radius: 50% (EXCEPTION to 0px rule -- dots are circular)
  active: #E8A838 (accent)
  shipped: #4ADE80 (green)
  coming-soon: #8A8A8E (text-secondary)
```

**Exception to 0px border-radius:** Status dots use `border-radius: 50%` because they are circular indicators, not rectangular UI elements. This is the only exception.

### TimelineBlock Layout (UICM-05)

```
Card:
  border: 1px solid var(--border-default)
  background: var(--bg-surface)
  padding: 24px (p-6)

Content stack (vertical):
  Row 1: Dates (font-mono, text-tertiary, 13px)
  Row 2: Role title (title, mt-1)
  Row 3: Company (body, text-secondary, mt-1)
  Row 4: Description (body, text-secondary, mt-2) -- optional, only if provided
```

**Timeline connector:** The connector (horizontal line on desktop, vertical on mobile) lives OUTSIDE the TimelineBlock component, in the page-level Timeline layout. The component is only the card. This keeps TimelineBlock reusable for both homepage (compact) and About page (if needed).

### Section Wrapper (UICM-07)

```
<section>
  padding-top: section-gap (clamp 80-160px)
  padding-bottom: section-gap (clamp 80-160px)
  padding-left: page-padding (clamp 20-80px)
  padding-right: page-padding (clamp 20-80px)

  <div> (inner container)
    max-width: 720px (default) / 1080px (wide)
    margin: 0 auto
    width: 100%

    {children}
  </div>
</section>
```

---

## Copywriting Contract

Phase 3 components are "dumb" -- they receive translated strings via props. The component library itself has no hardcoded copy. However, the /dev/components verification page needs sample data.

### Dev Page Sample Data (for verification only, not shipped)

| Component | Sample Props (EN) |
|-----------|-------------------|
| Button primary | "Get in touch ->" |
| Button secondary | "See the work ->" |
| Tag | "B2B SaaS" / "Payments" / "10+ years" |
| MetricCard | number: "99%+", label: "Uptime", context: "Payment success rate -- QubicaAMF" |
| CaseStudyCard | tag: "B2B SaaS", title: "How we reduced post-release incidents by 25%", preview: "Weekly cross-functional sessions with Product, Support and Engineering.", metric: "-25%" |
| ProjectCard | title: "CasaHunter", description: "Automated rental listing collection with AI analysis and Telegram alerts", techStack: ["Python", "Claude API", "Telegram"], status: "active", statusLabel: "Active" |
| TimelineBlock | role: "Product Manager", company: "QubicaAMF", dates: "2023 - today", description: "Payment systems, platform integrations and cashless solutions across 5 countries." |
| SectionHeader | label: "NUMBERS, NOT WORDS", title: "Numeri, non parole" |

### States

| State | Context | Copy |
|-------|---------|------|
| Empty state | Not applicable | Components always receive data via props; empty states are page-level concerns (Phase 4-5) |
| Error state | Not applicable | No data fetching in components; errors are page-level concerns |
| Destructive action | Not applicable | No destructive actions in Phase 3 |

**No CTA, empty state, error state, or destructive confirmation needed in Phase 3.** These are page-level concerns handled in Phase 4 and Phase 5.

---

## Responsive Breakpoints

| Breakpoint | Value | Component Behavior |
|------------|-------|-------------------|
| Mobile | < 768px (default) | Single column layouts, stacked card content, smaller padding |
| Tablet | >= 768px (md) | MetricCard 2-col grid, CaseStudyCard row layout with larger padding |
| Desktop | >= 1024px (lg) | MetricCard 3-col grid |

Specific responsive rules per component:

| Component | Mobile (<768px) | Tablet (>=768px) | Desktop (>=1024px) |
|-----------|----------------|-------------------|---------------------|
| MetricCard grid | 1 column | 2 columns | 3 columns |
| CaseStudyCard | Stacked (metric below text), p-8 | Row (metric right), md:p-12 | Same as tablet |
| ProjectCard | Full width | Full width | Full width (grid is page concern) |
| TimelineBlock | Stacked vertically | Stacked vertically | Horizontal layout (page concern) |
| Section (default) | max-width: 720px, page-padding | Same | Same |
| Section (wide) | max-width: 1080px, page-padding | Same | Same |
| Button | Full width (optional, page decides) | Auto width | Auto width |

---

## Dev Verification Route (D-04)

A `/dev/components` route that displays all Phase 3 components with their variants and states. This route is development-only and excluded from production build.

**Route:** `src/app/[locale]/dev/components/page.tsx`
**Production exclusion:** Wrap with `if (process.env.NODE_ENV === 'production') notFound()` at top of page.

**Page layout:**
- Section per component type, separated by horizontal rules
- Each section shows: component name, all variants, hover states visible on interaction
- Use actual sample data from the Copywriting Contract above
- MetricCard section shows the full 3x2 grid (6 cards) to verify the gap-[1px] pattern
- CaseStudyCard section shows 2 cards stacked to verify spacing
- Button section shows primary + secondary side by side

---

## Accessibility Requirements (Phase 3 scope)

| Requirement | Implementation |
|-------------|----------------|
| A11Y-02 Focus styles | `outline: 2px solid #E8A838`, `outline-offset: 2px` on Button (both variants) |
| A11Y-04 Semantic HTML | Cards use `<div>` (not interactive, not `<article>` unless standalone); Section uses `<section>`; Button renders `<button>` or `<a>` based on `href` prop |
| WCAG AA contrast | All text/bg combinations must pass 4.5:1. Key pairs: text-primary (#F5F5F0) on bg-primary (#0A0A0B) = 18.1:1 PASS; text-secondary (#8A8A8E) on bg-primary (#0A0A0B) = 5.3:1 PASS; text-tertiary (#5A5A5E) on bg-primary (#0A0A0B) = 3.2:1 BORDERLINE -- acceptable only for supplementary context text per WCAG AA large-text exception at 13px+ |
| Button a11y | When rendering as `<a>`: provide meaningful text content. When rendering as `<button>`: include `type="button"` default |
| Status dot | Status meaning conveyed via text label, not color alone (color-blind safe) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

**Note:** This project does not use shadcn or any component registry. All components are hand-built with Tailwind utilities. Registry safety is not applicable.

---

## Phase 3 Success Criteria (Visual)

| # | Criterion | How to Verify |
|---|-----------|---------------|
| 1 | Button primary renders with accent bg (#E8A838), dark text (#0A0A0B), 0px radius | Visual inspection on /dev/components |
| 2 | Button secondary renders with transparent bg, 1px border, text-primary | Visual inspection |
| 3 | Button primary hover: bg darkens to accent-hover, translates up 1px | Hover on /dev/components |
| 4 | Button secondary hover: border turns accent, text turns accent | Hover on /dev/components |
| 5 | Tag renders in JetBrains Mono, uppercase, tracking-wide, 13px | DevTools font inspector |
| 6 | MetricCard grid shows shared 1px borders (gap-[1px] pattern works) | Visual inspection of 6-card grid |
| 7 | MetricCard number is accent-colored, font-mono, large (clamp 40-72px) | Visual inspection |
| 8 | MetricCard hover: border color transitions to accent/40 | Hover on grid cells |
| 9 | CaseStudyCard shows tag+title+preview left, large metric right (desktop) | Visual inspection >= 768px |
| 10 | CaseStudyCard stacks metric below text on mobile | Resize to < 768px |
| 11 | CaseStudyCard hover: border accent + translate-y(-2px) | Hover on card |
| 12 | CaseStudyCard padding: p-8 mobile, md:p-12 desktop | DevTools spacing inspection |
| 13 | ProjectCard shows status dot + label, tech stack as tags | Visual inspection |
| 14 | ProjectCard hover: border accent | Hover on card |
| 15 | TimelineBlock shows dates in mono, role bold, company muted | Visual inspection |
| 16 | Section wrapper constrains content to 720px default, 1080px wide | DevTools max-width inspection |
| 17 | Section wrapper applies section-gap padding vertically | DevTools spacing inspection |
| 18 | SectionHeader shows monospace uppercase label above section title | Visual inspection |
| 19 | All elements have 0px border-radius (except status dots) | Visual inspection |
| 20 | /dev/components page is accessible and excluded from production | `NODE_ENV=production` returns 404 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
