---
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - src/app/[locale]/work/page.tsx
  - src/app/[locale]/lab/page.tsx
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03
  - COPY-01
  - MODB-01
  - MODB-02
  - MODB-04
---

# Plan 05-03: Work Page and Lab Page

## Objective

Build the Work page (headline, intro, 2 non-clickable CaseStudyCards with bilingual content) and the Lab page (headline, intro, CasaHunter expanded inline detail, MoneyMind "coming soon", PageCTA). Both pages consume Phase 3 UI components and i18n content from Plan 05-01.

## Tasks

<task id="05-03-01">
<title>Build Work page with 2 CaseStudyCards</title>
<read_first>
- src/i18n/messages/en.json (work namespace — verify it exists from 05-01)
- src/i18n/messages/it.json (work namespace)
- src/components/ui/case-study-card.tsx (CaseStudyCard component API — check exact props)
- src/components/layout/section.tsx (Section wrapper API — check wide variant)
- src/components/sections/page-cta.tsx (PageCTA API)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (Work Page layout contract)
- .planning/phases/05-inner-pages/05-CONTEXT.md (D-04: cards NOT clickable, D-05: only 2 cards)
- ../job-search-2026/selfrules-redesign/work-page.md (source copy for verification)
</read_first>
<actions>
Create src/app/[locale]/work/page.tsx as a Server Component:

1. **Boilerplate:** generateStaticParams, await params, setRequestLocale, getTranslations('work').

2. **Headline + Intro (Section default width 720px):**
   - h1: title size clamp(28-36px), Space Grotesk 700, text-primary
   - Intro paragraph: body size (16px), Inter 400, text-secondary, line-height 1.7, 24px below headline

3. **CaseStudyCards (Section wide-width 1080px, 48px from intro):**
   - 2 cards stacked vertically with 32px gap
   - Card 1 (Payments Rescue):
     - tag: "Payments · B2B SaaS · Enterprise"
     - title: from t('cases.payments.title')
     - preview: from t('cases.payments.preview')
     - metric: from t('cases.payments.metric')
     - NO href, NO onClick, NO cursor-pointer (D-04)
   - Card 2 (Cashless System):
     - tag: "Cashless · Product Strategy · Partnership"
     - title: from t('cases.cashless.title')
     - preview: from t('cases.cashless.preview')
     - metric: from t('cases.cashless.metric')
     - note: from t('cases.cashless.note') — [DRAFT]/[BOZZA] in label style (14px mono, text-secondary)
     - NO href (D-04)
   - Card 3 (LeadsBridge) does NOT exist in v1 (D-05) — do NOT render it
   - Cards still have hover: border-accent + translate-y(-2px) for visual polish (handled by CaseStudyCard component)

4. **PageCTA (80px below cards):**
   - text: t('cta.text')
   - primaryCta: { label: t('cta.primary'), href: '/about' } (or localized)
   - secondaryCta: { label: t('cta.secondary'), href: 'mailto:mattia@selfrules.org' }

CRITICAL:
- Cards are NOT clickable (D-04). No href prop, no role="link", no cursor-pointer.
- Only 2 cards (D-05). Do not render Card 3.
- Stories lead with the problem (MODB-04) — the source copy already does this, preserve verbatim.
</actions>
<acceptance_criteria>
- file src/app/[locale]/work/page.tsx exists
- grep "generateStaticParams" src/app/[locale]/work/page.tsx returns a match
- grep "setRequestLocale" src/app/[locale]/work/page.tsx returns a match
- grep "'work'" src/app/[locale]/work/page.tsx returns a match (namespace)
- grep "CaseStudyCard" src/app/[locale]/work/page.tsx returns a match
- grep "PageCTA" src/app/[locale]/work/page.tsx returns a match
- grep -c "CaseStudyCard" src/app/[locale]/work/page.tsx returns exactly 2 (or the import + 2 usages)
- grep "href.*work/payments\|href.*work/cashless\|role=\"link\"" src/app/[locale]/work/page.tsx returns ZERO matches (cards not clickable D-04)
- grep "LeadsBridge\|leadsbridge\|CS-03" src/app/[locale]/work/page.tsx returns ZERO matches (Card 3 not in v1 D-05)
- file does NOT contain 'use client'
- npm run build succeeds and work route shows circle icon (static)
</acceptance_criteria>
</task>

<task id="05-03-02">
<title>Build Lab page with CasaHunter expanded detail and MoneyMind</title>
<read_first>
- src/i18n/messages/en.json (lab namespace — verify it exists from 05-01)
- src/i18n/messages/it.json (lab namespace)
- src/components/ui/tag.tsx (Tag component API)
- src/components/layout/section.tsx (Section wrapper API)
- src/components/sections/page-cta.tsx (PageCTA API)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (Lab Page layout contract)
- .planning/phases/05-inner-pages/05-CONTEXT.md (D-06: CasaHunter expanded inline)
- ../job-search-2026/selfrules-redesign/lab-page.md (source copy for verification)
</read_first>
<actions>
Create src/app/[locale]/lab/page.tsx as a Server Component:

1. **Boilerplate:** generateStaticParams, await params, setRequestLocale, getTranslations('lab').

2. **Headline + Intro (Section default width 720px):**
   - h1: title size clamp(28-36px), Space Grotesk 700, text-primary
   - Intro: body size, Inter 400, text-secondary, line-height 1.7

3. **CasaHunter expanded section (D-06, Section default or wide width — 720px recommended for prose):**
   - Project title: subtitle size (20px), Space Grotesk 700, text-primary
   - One-liner: body size, text-secondary, 24px below title
   - Sub-sections, each with 32px vertical gap:
     - "The problem" / "Il problema": h3 subtitle 20px Space Grotesk 700, body 16px Inter
     - "What it does" / "Cosa fa": same pattern
     - "The product decision" / "La decisione di prodotto": same pattern
   - Stack tags: Use Tag components inline (14px mono, uppercase, text-secondary), 8px gap between tags. Render the stack list: Python 3.11, SQLite, Claude Sonnet, React 18, Tailwind, Leaflet, Recharts, Telegram API, GitHub Actions
   - Status line: label role (14px mono), accent-colored dot (small filled circle/square) + "Active. 880+ listings analyzed. I found the apartment." / IT equivalent
   - Links: GitHub and Dashboard live links — use href="#" with TODO comment for actual URLs. Style: text-secondary, hover text-accent

4. **MoneyMind "Coming soon" (64px below CasaHunter):**
   - Title: subtitle size (20px), Space Grotesk 700, text-primary ("MoneyMind")
   - One-liner: body size (16px), Inter 400, text-secondary
   - No card wrapper, no ProjectCard — just inline text (simpler than a card for a "coming soon" item)

5. **PageCTA (80px below):**
   - text: t('cta.text')
   - primaryCta: { label: t('cta.primary'), href: '/work' } (or localized)

NOTE: CasaHunter is an expanded inline detail section, NOT behind a link or separate page (D-06). All content renders directly on the Lab page. MoneyMind is minimal — just title + one-liner.
</actions>
<acceptance_criteria>
- file src/app/[locale]/lab/page.tsx exists
- grep "generateStaticParams" src/app/[locale]/lab/page.tsx returns a match
- grep "setRequestLocale" src/app/[locale]/lab/page.tsx returns a match
- grep "'lab'" src/app/[locale]/lab/page.tsx returns a match (namespace)
- grep "CasaHunter\|casahunter" src/app/[locale]/lab/page.tsx returns a match
- grep "MoneyMind\|moneymind" src/app/[locale]/lab/page.tsx returns a match
- grep "Tag" src/app/[locale]/lab/page.tsx returns a match (stack tags)
- grep "PageCTA" src/app/[locale]/lab/page.tsx returns a match
- file does NOT contain 'use client'
- grep -i "hire\|available.*remote\|download cv" src/app/[locale]/lab/page.tsx returns ZERO matches (MODB)
- npm run build succeeds and lab route shows circle icon (static)
</acceptance_criteria>
</task>

<task id="05-03-03">
<title>Visual verification — Work and Lab pages</title>
<read_first>
- src/app/[locale]/work/page.tsx (just built)
- src/app/[locale]/lab/page.tsx (just built)
- .planning/phases/05-inner-pages/05-UI-SPEC.md (layout contracts)
</read_first>
<actions>
Run `npm run dev` and verify in browser:

**Work page (/work and /it/work):**
1. Headline and intro render correctly with proper spacing
2. 2 CaseStudyCards visible, stacked with 32px gap
3. Card 1 shows "Payments · B2B SaaS · Enterprise" tag and metric "99.19% success rate..."
4. Card 2 shows [DRAFT]/[BOZZA] note in label style
5. Cards are NOT clickable — no cursor change on hover, no link behavior
6. Cards DO have visual hover effect (border-accent, translate-y)
7. No Card 3 (LeadsBridge) anywhere
8. PageCTA with 2 buttons: "About me"/"Chi sono" + "Get in touch"/"Scrivimi"
9. Both locales render complete content

**Lab page (/lab and /it/lab):**
1. Headline and intro render correctly
2. CasaHunter section shows all 5 sub-sections: one-liner, problem, what it does, product decision, stack+status
3. Stack tags render as inline Tag components (mono, uppercase)
4. Status shows active indicator
5. MoneyMind appears as simple text (title + one-liner), 64px below CasaHunter
6. PageCTA with "See the work" / "Guarda i lavori" button
7. Both locales render complete content

Fix any issues found before marking complete.
</actions>
<acceptance_criteria>
- npm run dev starts without errors
- /work renders without console errors
- /it/work renders with Italian content
- /lab renders without console errors
- /it/lab renders with Italian content
- npm run build succeeds with both routes showing circle icons (static)
</acceptance_criteria>
</task>
