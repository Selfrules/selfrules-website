---
phase: 05-inner-pages
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/[locale]/work/page.tsx
  - src/app/[locale]/lab/page.tsx
autonomous: true
requirements: [PAGE-02, PAGE-03, MODB-01]

must_haves:
  truths:
    - "Work page renders headline, intro, 2 CaseStudyCards (NOT clickable per D-04), and PageCTA in both locales"
    - "Lab page renders headline, intro, CasaHunter expanded inline detail (D-06), MoneyMind coming soon, and PageCTA in both locales"
    - "Only 2 case study cards appear on Work page -- no third card (D-05)"
    - "CaseStudyCard 2 shows [DRAFT]/[BOZZA] note"
    - "No job-seeker framing on either page (MODB-01)"
  artifacts:
    - path: "src/app/[locale]/work/page.tsx"
      provides: "Work page with 2 CaseStudyCards and PageCTA"
      exports: ["default"]
      min_lines: 50
    - path: "src/app/[locale]/lab/page.tsx"
      provides: "Lab page with CasaHunter detail, MoneyMind, PageCTA"
      exports: ["default"]
      min_lines: 70
  key_links:
    - from: "src/app/[locale]/work/page.tsx"
      to: "src/components/ui/case-study-card.tsx"
      via: "import CaseStudyCard"
      pattern: "import.*CaseStudyCard"
    - from: "src/app/[locale]/work/page.tsx"
      to: "src/i18n/messages/en.json"
      via: "getTranslations('work')"
      pattern: "getTranslations.*work"
    - from: "src/app/[locale]/lab/page.tsx"
      to: "src/i18n/messages/en.json"
      via: "getTranslations('lab')"
      pattern: "getTranslations.*lab"
---

<objective>
Build the Work and Lab pages -- the two card-based/project-detail pages. Work uses CaseStudyCard components at wide width. Lab uses expanded inline detail for CasaHunter.

Purpose: Work demonstrates competence through case studies. Lab shows personal projects with the same rigor. Both use Phase 3 components (CaseStudyCard, Tag) and share the PageCTA pattern.
Output: Two complete, bilingual inner pages consuming Phase 3 UI primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-inner-pages/05-CONTEXT.md
@.planning/phases/05-inner-pages/05-UI-SPEC.md
@.planning/phases/05-inner-pages/05-RESEARCH.md
@.planning/phases/05-inner-pages/05-01-SUMMARY.md

@../job-search-2026/selfrules-redesign/work-page.md
@../job-search-2026/selfrules-redesign/lab-page.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Work page -- headline, intro, 2 CaseStudyCards, CTA</name>
  <files>src/app/[locale]/work/page.tsx</files>

  <read_first>
    - src/i18n/messages/en.json (work namespace from Plan 01)
    - src/components/ui/case-study-card.tsx (CaseStudyCard props interface -- tag, title, preview, metric, note)
    - src/components/sections/page-cta.tsx (PageCTA props)
    - src/components/layout/section.tsx (Section component, maxWidth="wide" for 1080px)
    - src/app/[locale]/about/page.tsx (established page pattern if Plan 02 already ran)
    - ../job-search-2026/selfrules-redesign/work-page.md (source copy for verification)
  </read_first>

  <action>
Create `src/app/[locale]/work/page.tsx` as an async Server Component.

**Structure:**

1. **Boilerplate:** `generateStaticParams` with 'en'/'it'. `await params`, `setRequestLocale(locale)`, `const t = await getTranslations('work');`.

2. **Headline + Intro:** Inside `<Section>` at default 720px:
   - `<h1 className="text-[clamp(28px,3vw,36px)] font-bold leading-[1.2] text-primary">{t('headline')}</h1>`
   - `<p className="mt-6 text-base leading-[1.7] text-secondary">{t('intro')}</p>`

3. **CaseStudyCards:** Inside `<Section maxWidth="wide">` (1080px). Cards stacked vertically with 32px gap (`space-y-8`). 48px from intro (`mt-12`).

   **Card 1 -- Payments Rescue:**
   ```tsx
   <CaseStudyCard
     tag={t('cases.payments.tag')}
     title={t('cases.payments.title')}
     preview={t('cases.payments.preview')}
     metric={t('cases.payments.metric')}
   />
   ```

   **Card 2 -- Cashless System:**
   ```tsx
   <CaseStudyCard
     tag={t('cases.cashless.tag')}
     title={t('cases.cashless.title')}
     preview={t('cases.cashless.preview')}
     metric={t('cases.cashless.metric')}
     note={t('cases.cashless.note')}
   />
   ```

   **CRITICAL per D-04:** Do NOT pass `href` prop to CaseStudyCard. Cards are NOT clickable in v1. No cursor-pointer, no role="link". They are informational containers only.

   **Per D-05:** Only 2 cards. Do NOT render a third card for LeadsBridge. The "Future section" from the source copy is excluded.

4. **PageCTA:** `<PageCTA text={t('cta.text')} primaryCta={{ label: t('cta.primary'), href: '/about' }} secondaryCta={{ label: t('cta.secondary'), href: 'mailto:mattia@selfrules.org' }} />`
   Adjust hrefs for locale awareness -- if in IT locale, the About link should be `/it/about`. Check how other pages handle locale-prefixed internal links.

All wrapped in `<main>`.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/app/[locale]/work/page.tsx exists and exports default async function
    - File contains `generateStaticParams` with 'en' and 'it'
    - File contains `setRequestLocale(locale)` before getTranslations
    - File contains `getTranslations('work')`
    - File imports CaseStudyCard component
    - File renders exactly 2 CaseStudyCard instances (payments + cashless)
    - Neither CaseStudyCard has an `href` prop (D-04: not clickable)
    - Second card passes `note` prop containing the draft note
    - File does NOT reference "leadsbridge" or a third case study (D-05)
    - File imports and uses PageCTA with both primary and secondary CTAs
    - `npm run build` succeeds with work route as static
  </acceptance_criteria>

  <done>Work page renders headline, intro, 2 non-clickable CaseStudyCards (Payments + Cashless with BOZZA/DRAFT note), and PageCTA with primary + secondary buttons. Only 2 cards, no third.</done>
</task>

<task type="auto">
  <name>Task 2: Lab page -- CasaHunter expanded detail, MoneyMind coming soon, CTA</name>
  <files>src/app/[locale]/lab/page.tsx</files>

  <read_first>
    - src/i18n/messages/en.json (lab namespace from Plan 01)
    - src/components/ui/tag.tsx (Tag component for stack tags)
    - src/components/sections/page-cta.tsx (PageCTA props)
    - src/components/layout/section.tsx (Section component)
    - src/app/[locale]/work/page.tsx (sibling page pattern from Task 1)
    - ../job-search-2026/selfrules-redesign/lab-page.md (source copy for verification)
  </read_first>

  <action>
Create `src/app/[locale]/lab/page.tsx` as an async Server Component.

**Structure per D-06 (CasaHunter as expanded inline section):**

1. **Boilerplate:** `generateStaticParams`, `await params`, `setRequestLocale(locale)`, `const t = await getTranslations('lab');`.

2. **Headline + Intro:** Inside `<Section>` at default 720px:
   - `<h1 className="text-[clamp(28px,3vw,36px)] font-bold leading-[1.2] text-primary">{t('headline')}</h1>`
   - `<p className="mt-6 text-base leading-[1.7] text-secondary">{t('intro')}</p>`

3. **CasaHunter expanded section (D-06):** Inside `<Section>` at default 720px. 48px from intro (`mt-12`).
   - Project title: `<h2 className="text-xl font-bold leading-[1.3] text-primary">{t('casahunter.title')}</h2>`
   - One-liner: `<p className="mt-6 text-base text-secondary">{t('casahunter.oneliner')}</p>`
   - Three sub-sections (problem, whatItDoes, productDecision), each with 32px gap between them (`mt-8`):
     - Sub-heading: `<h3 className="text-xl font-bold leading-[1.3] text-primary">{t('casahunter.problem.label')}</h3>`
     - Body: `<p className="mt-4 text-base leading-[1.6] text-primary">{t('casahunter.problem.text')}</p>`
   - **Stack tags:** Render using Tag components inline. Map over `t.raw('casahunter.stack')` array (use `t.raw()` to get the raw array). Wrap in flex container with 8px gap:
     ```tsx
     <div className="mt-8 flex flex-wrap gap-2">
       {(t.raw('casahunter.stack') as string[]).map((tech) => (
         <Tag key={tech} label={tech} />
       ))}
     </div>
     ```
   - **Status line:** `<p className="mt-4 font-mono text-sm uppercase tracking-wide text-secondary"><span className="mr-2 inline-block h-2 w-2 rounded-full bg-accent"></span>{t('casahunter.status')}</p>` -- green accent dot for active status.
   - **Links:** `<div className="mt-4 flex gap-4 text-base text-secondary"><a href={t('casahunter.githubUrl')} className="transition-colors duration-150 hover:text-accent">GitHub</a><span>·</span><a href={t('casahunter.dashboardUrl')} className="transition-colors duration-150 hover:text-accent">{locale === 'it' ? 'Dashboard live' : 'Live dashboard'}</a></div>` -- placeholder hrefs are "#" with TODO comment for real URLs.

4. **MoneyMind "Coming soon":** 64px below CasaHunter section (`mt-16`).
   - `<h2 className="text-xl font-bold leading-[1.3] text-primary">{t('comingSoon.title')}</h2>`
   - `<p className="mt-4 text-base text-secondary">{t('comingSoon.text')}</p>`

5. **PageCTA:** `<PageCTA text={t('cta.text')} primaryCta={{ label: t('cta.primary'), href: '/work' }} />`
   Locale-aware href for /work.

All wrapped in `<main>`.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/app/[locale]/lab/page.tsx exists and exports default async function
    - File contains `generateStaticParams` with 'en' and 'it'
    - File contains `setRequestLocale(locale)` before getTranslations
    - File contains `getTranslations('lab')`
    - File renders CasaHunter title, oneliner, problem, whatItDoes, productDecision sections
    - File uses Tag component for stack rendering OR renders monospace tags inline
    - File renders status with accent-colored dot indicator
    - File renders MoneyMind section with title and text
    - File imports and uses PageCTA
    - File does NOT use ProjectCard for CasaHunter (it's expanded inline per D-06)
    - `npm run build` succeeds with lab route as static
  </acceptance_criteria>

  <done>Lab page renders CasaHunter as expanded inline detail (D-06) with all sub-sections, stack tags, active status, and placeholder links. MoneyMind appears as coming soon. PageCTA links to Work page.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with both /work and /lab routes as static
- Visit /work -- headline, intro, exactly 2 case study cards (no third), cards not clickable, Card 2 has draft note
- Visit /lab -- headline, intro, CasaHunter full detail inline, MoneyMind coming soon, CTA
- No "job seeker" language on either page
- Check IT locale (/it/work, /it/lab) renders Italian copy
</verification>

<success_criteria>
Work page shows exactly 2 non-clickable CaseStudyCards with correct copy. Lab page shows CasaHunter as expanded inline section with all detail. Both pages have correct PageCTA and bilingual content.
</success_criteria>

<output>
After completion, create `.planning/phases/05-inner-pages/05-03-SUMMARY.md`
</output>
