---
phase: 05-inner-pages
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/[locale]/about/page.tsx
  - src/app/[locale]/approach/page.tsx
autonomous: true
requirements: [PAGE-01, PAGE-04, MODB-03, MODB-04]

must_haves:
  truths:
    - "About page renders headline, 4 career blocks as prose, 3 principles as prose, personal section, and PageCTA in both locales"
    - "Approach page renders headline, intro, 5 numbered prose sections, closing text, and PageCTA in both locales"
    - "Bold inline metrics render in accent color on both pages"
    - "No job-seeker framing on either page -- stories lead with problem/result (MODB-04)"
  artifacts:
    - path: "src/app/[locale]/about/page.tsx"
      provides: "About page with 4 career blocks, 3 principles, personal section, CTA"
      exports: ["default"]
      min_lines: 80
    - path: "src/app/[locale]/approach/page.tsx"
      provides: "Approach page with 5 numbered sections, closing, CTA"
      exports: ["default"]
      min_lines: 60
  key_links:
    - from: "src/app/[locale]/about/page.tsx"
      to: "src/i18n/messages/en.json"
      via: "getTranslations('about')"
      pattern: "getTranslations.*about"
    - from: "src/app/[locale]/about/page.tsx"
      to: "src/components/sections/page-cta.tsx"
      via: "import PageCTA"
      pattern: "import.*PageCTA"
    - from: "src/app/[locale]/approach/page.tsx"
      to: "src/i18n/messages/en.json"
      via: "getTranslations('approach')"
      pattern: "getTranslations.*approach"
---

<objective>
Build the About and Approach pages -- the two prose-heavy editorial pages that share the same 720px max-width layout and use no card components.

Purpose: About is the most content-rich page, establishing career narrative. Approach codifies the working method. Both are pure prose at 720px, making them a natural pair. They use the same rendering patterns (rich text for bold metrics, Section wrapper, PageCTA).
Output: Two complete, bilingual inner pages with all copy from source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-inner-pages/05-CONTEXT.md
@.planning/phases/05-inner-pages/05-UI-SPEC.md
@.planning/phases/05-inner-pages/05-RESEARCH.md
@.planning/phases/05-inner-pages/05-01-SUMMARY.md

@../job-search-2026/selfrules-redesign/about-page.md
@../job-search-2026/selfrules-redesign/approach-page.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: About page -- career blocks, principles, personal section, CTA</name>
  <files>src/app/[locale]/about/page.tsx</files>

  <read_first>
    - src/i18n/messages/en.json (about namespace structure from Plan 01)
    - src/components/sections/page-cta.tsx (PageCTA props interface)
    - src/components/layout/section.tsx (Section component API, maxWidth prop)
    - src/components/sections/section-header.tsx (SectionHeader for section labels)
    - src/app/[locale]/page.tsx (existing page pattern for generateStaticParams, setRequestLocale, await params)
    - ../job-search-2026/selfrules-redesign/about-page.md (source copy for verification)
  </read_first>

  <action>
Create `src/app/[locale]/about/page.tsx` as an async Server Component.

**Structure per D-01 and D-02 (prose sections, NOT cards):**

1. **Boilerplate:** Export `generateStaticParams` returning `[{ locale: 'en' }, { locale: 'it' }]`. In default function: `const { locale } = await params;` then `setRequestLocale(locale);` then `const t = await getTranslations('about');`.

2. **Headline:** `<h1>` with `text-[clamp(28px,3vw,36px)] font-bold leading-[1.2] text-primary`. Content: `t('headline')`.

3. **Career blocks (4):** Each block is a `<section>` with `mb-12` (48px gap between blocks). Inside each:
   - `<h2 className="text-xl font-bold leading-[1.3] text-primary">{t('career.selfrules.company')}</h2>`
   - `<span className="mt-1 block font-mono text-sm uppercase tracking-wide text-secondary">{t('career.selfrules.dates')}</span>`
   - For the text, use `t.rich('career.selfrules.text', { bold: (chunks) => <strong className="font-bold text-accent">{chunks}</strong> })` to render `<bold>` tags as accent-colored bold text.
   - Text paragraphs: split on `\n\n` and render each as `<p className="mt-4 text-base leading-[1.6] text-primary">`. The `t.rich()` return handles the entire text block.

   Career block order: selfrules, flowing, leadsbridge, qubicamf.

4. **"Cosa credo" / "What I believe" section (D-02):** Add 64px gap from career blocks (`mt-16`). SectionHeader with label `t('beliefs.label')`. Then 3 principles as sequential prose:
   - Each: `<h2 className="text-xl font-bold leading-[1.3] text-primary">{t('beliefs.belief1.title')}</h2>`
   - `<p className="mt-4 text-base leading-[1.6] text-primary">{t('beliefs.belief1.text')}</p>`
   - 48px gap between principles (`mb-12`).

5. **"Fuori dal lavoro" / "Outside work" section (D-03, Claude's discretion):** Add 64px gap (`mt-16`). Use a subtle visual separator -- a `<div>` with `border-t border-[var(--color-border-default)] pt-16` to gently delineate this personal section from the professional content. Then:
   - `<h2 className="text-xl font-bold leading-[1.3] text-primary">{t('outside.label')}</h2>`
   - `<p className="mt-4 text-base leading-[1.6] text-primary">{t('outside.text')}</p>`

6. **PageCTA:** `<PageCTA text={t('cta.text')} primaryCta={{ label: t('cta.primary'), href: 'mailto:mattia@selfrules.org' }} />`

All content within `<Section>` wrapper at default 720px max-width. The entire page is wrapped in `<main>`.

**MODB-04 compliance:** Career blocks lead with what happened (the studio, the project, the product), not "I worked on". This is already guaranteed by the source copy -- just use it verbatim via i18n.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/app/[locale]/about/page.tsx exists and exports a default async function
    - File contains `generateStaticParams` returning locales 'en' and 'it'
    - File contains `setRequestLocale(locale)` called before getTranslations
    - File contains `getTranslations('about')`
    - File contains `t.rich(` for career blocks with bold metrics
    - File contains `font-bold text-accent` for the bold rich text callback
    - File renders 4 career blocks: selfrules, flowing, leadsbridge, qubicamf
    - File renders 3 belief sections: belief1, belief2, belief3
    - File renders outside section with label
    - File imports and uses PageCTA component
    - File does NOT contain "hire", "looking for", "job" (MODB compliance)
    - `npm run build` succeeds with about route as static (circle icon)
  </acceptance_criteria>

  <done>About page renders complete in both EN and IT with 4 career blocks as prose (D-01), 3 principles as prose (D-02), personal section with border separator (D-03), and PageCTA. Bold metrics display in accent color.</done>
</task>

<task type="auto">
  <name>Task 2: Approach page -- 5 numbered prose sections, closing, CTA</name>
  <files>src/app/[locale]/approach/page.tsx</files>

  <read_first>
    - src/i18n/messages/en.json (approach namespace structure)
    - src/app/[locale]/about/page.tsx (pattern from Task 1 for consistency)
    - src/components/sections/page-cta.tsx (PageCTA props)
    - src/components/layout/section.tsx (Section component)
    - ../job-search-2026/selfrules-redesign/approach-page.md (source copy for verification)
  </read_first>

  <action>
Create `src/app/[locale]/approach/page.tsx` as an async Server Component.

**Structure:**

1. **Boilerplate:** Same pattern as About -- `generateStaticParams`, `await params`, `setRequestLocale(locale)`, `const t = await getTranslations('approach');`.

2. **Headline:** `<h1 className="text-[clamp(28px,3vw,36px)] font-bold leading-[1.2] text-primary">{t('headline')}</h1>`

3. **Intro:** `<p className="mt-6 text-base leading-[1.7] text-secondary">{t('intro')}</p>` -- 24px below headline, text-secondary per UI-SPEC.

4. **5 numbered sections:** 48px gap from intro to first section (`mt-12`). Each section with 64px gap between them (`mb-16`). For each section (s1 through s5):
   - `<h2 className="text-xl font-bold leading-[1.3] text-primary">{t('sections.s1.title')}</h2>`
   - Use `t.rich('sections.s1.text', { bold: (chunks) => <strong className="font-bold text-accent">{chunks}</strong> })` for sections that contain bold metrics (s2 has `<bold>35%</bold>`).
   - For sections without bold markers, plain `t('sections.s3.text')` is sufficient but using `t.rich()` universally is safer (it will just pass through text without bold tags).
   - Text: render paragraphs by splitting on `\n\n` -- each as `<p className="mt-4 text-base leading-[1.6] text-primary">`.

   The numbered titles (e.g., "1. Understand before you touch anything") already contain the number in the copy text. No separate visual number marker needed per UI-SPEC decision.

5. **Closing text:** 64px above CTA. `<p className="mt-16 text-base leading-[1.7] text-primary">{t('closing')}</p>`

6. **PageCTA:** `<PageCTA text={t('cta.text')} primaryCta={{ label: t('cta.primary'), href: '/work' }} />`
   For IT locale the href should be `/it/work`. Use the Link component from next-intl or construct the path based on locale. Check existing page patterns for how locale-aware hrefs are built (likely using `usePathname` or template literals with locale).

All content within `<Section>` at default 720px max-width. Wrapped in `<main>`.

**MODB-03 compliance:** The page speaks about how decisions are made, not why anyone should hire. This is guaranteed by the source copy.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/app/[locale]/approach/page.tsx exists and exports a default async function
    - File contains `generateStaticParams` returning locales 'en' and 'it'
    - File contains `setRequestLocale(locale)` before getTranslations
    - File contains `getTranslations('approach')`
    - File renders all 5 sections: references t('sections.s1') through t('sections.s5')
    - File contains `t.rich(` for at least section s2 (has bold 35%)
    - File renders closing text via t('closing')
    - File imports and uses PageCTA
    - File does NOT contain "hire", "looking for", "job" (MODB compliance)
    - `npm run build` succeeds with approach route as static (circle icon)
  </acceptance_criteria>

  <done>Approach page renders complete in both EN and IT with intro, 5 numbered prose sections at 720px, bold metrics in accent, closing paragraph, and PageCTA linking to Work page.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with both /about and /approach routes as static
- Visit /about and /it/about -- all career blocks, principles, personal section, CTA visible
- Visit /approach and /it/approach -- intro, 5 numbered sections, closing, CTA visible
- Bold metrics (35%, 99%+, -25%, 116, 5 new) render in accent color
- No "job seeker" language on either page
</verification>

<success_criteria>
About and Approach pages render complete bilingual content matching source files. Career blocks display as prose sections (D-01), not cards. Principles display as sequential prose (D-02). Bold metrics render in accent. All content at 720px max-width.
</success_criteria>

<output>
After completion, create `.planning/phases/05-inner-pages/05-02-SUMMARY.md`
</output>
