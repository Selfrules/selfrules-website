---
wave: 2
depends_on: ["04-01-PLAN"]
files_modified:
  - src/components/sections/HowIWork.tsx
  - src/components/sections/Timeline.tsx
  - src/components/sections/Metrics.tsx
  - src/components/sections/CurrentWork.tsx
  - src/components/sections/Contact.tsx
autonomous: true
requirements: [HOME-02, HOME-03, HOME-04, HOME-05, HOME-06]
---

# Plan 04-02: Section Components (HowIWork, Timeline, Metrics, CurrentWork, Contact)

## Objective

Build the remaining 5 homepage section components. Each is a Server Component that receives pre-translated strings as props and composes Phase 3 UI primitives. After this plan, all 6 section components exist and are ready to be wired into page.tsx.

---

## Task 1: HowIWork section component (HOME-02)

<task id="04-02-T1">
<read_first>
- .planning/phases/04-homepage/04-UI-SPEC.md (Layout Contract → How I Work Section)
- .planning/phases/04-homepage/04-UI-SPEC.md (Typography table — all 4 roles)
- ../job-search-2026/selfrules-redesign/homepage.md (Section 2 EN + IT copy for reference)
- src/components/sections/SectionHeader.tsx (SectionHeader API from Plan 04-01)
- src/components/layout/Section.tsx (Section wrapper API — if exists, check maxWidth prop)
</read_first>

**Action:** Create `src/components/sections/HowIWork.tsx` as a Server Component.

**Props interface:**
```typescript
interface HowIWorkProps {
  label: string;
  title: string;
  intro: string;
  pillars: Array<{
    title: string;
    text: React.ReactNode;  // ReactNode to support rich text with <b> tags
  }>;
}
```

**Layout:**
- Use `Section` wrapper (default width 720px for intro, or wide for pillars — Claude decides)
- `SectionHeader` with label + title
- Intro paragraph: `<p>`, body size 16px, leading-[1.7], text-secondary, max-w-[720px], margin-bottom 48px
- Pillar grid: `grid grid-cols-1 lg:grid-cols-3 gap-x-8 gap-y-12` (3 columns at >=1024px, 1 column mobile)
- Each pillar:
  - Title: `<h3>`, font-heading (Space Grotesk), font-bold, text-[clamp(28px,3vw,36px)] or a smaller title size, leading-[1.2], text-primary, margin-bottom 16px
  - Text: `<p>`, body size 16px, leading-[1.6], text-secondary. Rendered as ReactNode to support `<b>` rich text.
- No background, no border on pillars (per UI-SPEC)

**Typography constraints:**
- Only weights 400 and 700
- Pillar titles use Title role: font-heading font-bold
- Pillar text uses Body role: font-sans font-normal

<acceptance_criteria>
- File exists at src/components/sections/HowIWork.tsx
- grep `"use client"` src/components/sections/HowIWork.tsx returns 0 matches (Server Component)
- grep `export function HowIWork` src/components/sections/HowIWork.tsx returns 1 match
- grep `SectionHeader` src/components/sections/HowIWork.tsx returns at least 1 match
- grep `lg:grid-cols-3` src/components/sections/HowIWork.tsx returns at least 1 match (3-column desktop layout)
- grep `grid-cols-1` src/components/sections/HowIWork.tsx returns at least 1 match (single column mobile)
- grep `font-heading` src/components/sections/HowIWork.tsx returns at least 1 match (Space Grotesk for pillar titles)
- grep `<h3` src/components/sections/HowIWork.tsx returns at least 1 match (semantic heading for pillars)
- grep `text-secondary` src/components/sections/HowIWork.tsx returns at least 1 match
- TypeScript compiles without errors
</acceptance_criteria>
</task>

---

## Task 2: Timeline section component (HOME-03)

<task id="04-02-T2">
<read_first>
- .planning/phases/04-homepage/04-UI-SPEC.md (Layout Contract → Journey Section)
- .planning/phases/04-homepage/04-CONTEXT.md (Claude's discretion: timeline overlap, mobile connector, closing phrase placement)
- DESIGN-UPDATE-v25.md (Change 7: vertical timeline with square nodes, overlap badge, last node amber)
- src/components/ui/TimelineBlock.tsx (existing TimelineBlock API from Phase 3 — if exists)
- src/components/sections/SectionHeader.tsx (SectionHeader API)
</read_first>

**Action:** Create `src/components/sections/Timeline.tsx` as a Server Component.

**Props interface:**
```typescript
interface TimelineProps {
  label: string;
  title: string;
  blocks: Array<{
    role: string;
    dates: string;
    text: React.ReactNode;  // ReactNode for rich text with <b> tags
  }>;
  closingLine: string;
}
```

**Layout:**
- `SectionHeader` with label + title
- Desktop (>=1024px): horizontal timeline with 4 blocks in a row, connected by a 1px line in border-default color
- Mobile (<1024px): vertical timeline with connector line on the left
- Per DESIGN-UPDATE-v25.md: timeline nodes are SQUARE (8-10px, border-radius: 0 — consistent with site's 0px radius identity)
- Last node (Product Manager) is filled amber (#E8A838)
- Previous nodes are outlined (1px border, transparent fill)
- Overlapping dates (Designer 2012-18 and Developer 2016-20): equal columns, dates speak for themselves
- Each block:
  - Role: `<h3>`, font-heading, font-bold, text-primary
  - Dates: font-mono, text-sm, text-secondary, uppercase NOT applied (dates are not labels per se)
  - Text: body size, text-secondary, ReactNode for rich text
- Closing phrase: body 16px, leading-[1.7], text-primary, mt-12, max-w-[720px]
- If TimelineBlock component exists from Phase 3, compose with it. If not, build the timeline layout directly.

**Connector implementation:**
- Horizontal (desktop): 1px line `h-px bg-border` running between nodes
- Vertical (mobile): 2px line `w-[2px] bg-border` on the left side, nodes positioned on the line

<acceptance_criteria>
- File exists at src/components/sections/Timeline.tsx
- grep `"use client"` src/components/sections/Timeline.tsx returns 0 matches (Server Component)
- grep `export function Timeline` src/components/sections/Timeline.tsx returns 1 match
- grep `SectionHeader` src/components/sections/Timeline.tsx returns at least 1 match
- grep `font-heading` src/components/sections/Timeline.tsx returns at least 1 match (Space Grotesk for role titles)
- grep `font-mono` src/components/sections/Timeline.tsx returns at least 1 match (dates in monospace)
- grep `closingLine` src/components/sections/Timeline.tsx returns at least 1 match
- grep `bg-accent\|bg-\[#E8A838\]\|amber` src/components/sections/Timeline.tsx returns at least 1 match (last node amber filled)
- grep `border-radius` src/components/sections/Timeline.tsx returns 0 matches (nodes are square by default, 0px radius via global reset)
- grep `lg:` src/components/sections/Timeline.tsx returns at least 1 match (responsive breakpoint)
- TypeScript compiles without errors
</acceptance_criteria>
</task>

---

## Task 3: Metrics section component (HOME-04)

<task id="04-02-T3">
<read_first>
- .planning/phases/04-homepage/04-UI-SPEC.md (Layout Contract → Numbers Section)
- .planning/phases/04-homepage/04-UI-SPEC.md (Component Inventory — MetricCard, CountUpNumber)
- .planning/phases/03-ui-primitives/03-CONTEXT.md (D-03: MetricCard grid gap-[1px] pattern — CHECK if this is still the pattern or if gap-6/gap-[24px] per UI-SPEC)
- src/components/ui/MetricCard.tsx (existing MetricCard API from Phase 3)
- src/components/sections/SectionHeader.tsx (SectionHeader API)
- src/components/layout/Section.tsx (Section wrapper — wide variant)
</read_first>

**Action:** Create `src/components/sections/Metrics.tsx` as a Server Component.

**Props interface:**
```typescript
interface MetricsProps {
  label: string;
  title: string;
  metrics: Array<{
    number: string;
    context: string;
  }>;
  linkText: string;
  linkHref: string;
}
```

**Layout:**
- Use `Section` wrapper with `wide` prop (1080px max-width)
- `SectionHeader` with label + title
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6` (3x2 desktop, 2x3 tablet, 1 column mobile)
  - NOTE: UI-SPEC says gap 24px (gap-6). Check Phase 3 D-03 — if MetricCard uses the gap-[1px] bg-border shared-cell pattern, use that instead. The UI-SPEC takes precedence.
- Each MetricCard rendered with `number` and `context` props
- MetricCard numbers use CountUpNumber if available from Phase 3. If CountUpNumber doesn't exist yet, pass numbers as-is — the animation will be added in Phase 6 or during component creation.
- Below grid: text link with arrow suffix
  - `<a>`, text-secondary, hover:text-accent, transition-colors duration-150
  - Text: `{linkText} →` (arrow is a literal → character)

**Grid pattern decision:**
- If Phase 3 MetricCard uses gap-[1px] with bg-border container: use `gap-[1px] bg-border border border-default` on grid container, with `bg-primary` on each MetricCard
- If MetricCard is standalone with its own border: use `gap-6` standard spacing

<acceptance_criteria>
- File exists at src/components/sections/Metrics.tsx
- grep `"use client"` src/components/sections/Metrics.tsx returns 0 matches (Server Component)
- grep `export function Metrics` src/components/sections/Metrics.tsx returns 1 match
- grep `SectionHeader` src/components/sections/Metrics.tsx returns at least 1 match
- grep `lg:grid-cols-3` src/components/sections/Metrics.tsx returns at least 1 match (3-column desktop)
- grep `sm:grid-cols-2` src/components/sections/Metrics.tsx returns at least 1 match (2-column tablet)
- grep `MetricCard` src/components/sections/Metrics.tsx returns at least 1 match (consumes Phase 3 component)
- grep `linkText\|linkHref` src/components/sections/Metrics.tsx returns at least 1 match
- grep `hover:text-accent` src/components/sections/Metrics.tsx returns at least 1 match (link hover state)
- TypeScript compiles without errors
</acceptance_criteria>
</task>

---

## Task 4: CurrentWork section component (HOME-05)

<task id="04-02-T4">
<read_first>
- .planning/phases/04-homepage/04-UI-SPEC.md (Layout Contract → Current Work Section)
- .planning/phases/04-homepage/04-UI-SPEC.md (Interaction States → "Now" card)
- src/components/sections/SectionHeader.tsx (SectionHeader API)
- src/components/layout/Section.tsx (Section wrapper)
</read_first>

**Action:** Create `src/components/sections/CurrentWork.tsx` as a Server Component.

**Props interface:**
```typescript
interface CurrentWorkProps {
  label: string;
  title: string;
  cards: Array<{
    title: string;
    text: string;
  }>;
}
```

**Layout:**
- `SectionHeader` with label + title
- Card grid: `grid grid-cols-1 md:grid-cols-2 gap-6` (2 cards side by side at >=768px, stacked on mobile)
- Each card:
  - Container: `<div>`, border border-default, bg-surface (#111113), p-6, transition-all duration-150
  - Hover: `hover:border-accent hover:-translate-y-0.5` (border-accent + translate-y(-2px))
  - Title: `<h3>`, font-heading, font-bold, text-primary, mb-3
  - Text: `<p>`, body 16px, leading-[1.6], text-secondary
- 0px border-radius (enforced globally, no rounded classes)

<acceptance_criteria>
- File exists at src/components/sections/CurrentWork.tsx
- grep `"use client"` src/components/sections/CurrentWork.tsx returns 0 matches (Server Component)
- grep `export function CurrentWork` src/components/sections/CurrentWork.tsx returns 1 match
- grep `SectionHeader` src/components/sections/CurrentWork.tsx returns at least 1 match
- grep `md:grid-cols-2` src/components/sections/CurrentWork.tsx returns at least 1 match (2-column desktop)
- grep `bg-surface` src/components/sections/CurrentWork.tsx returns at least 1 match (card background)
- grep `border-default\|border-border` src/components/sections/CurrentWork.tsx returns at least 1 match (card border)
- grep `hover:border-accent` src/components/sections/CurrentWork.tsx returns at least 1 match (hover state)
- grep `hover:-translate-y` src/components/sections/CurrentWork.tsx returns at least 1 match (hover translate)
- grep `font-heading` src/components/sections/CurrentWork.tsx returns at least 1 match (Space Grotesk for card titles)
- grep `<h3` src/components/sections/CurrentWork.tsx returns at least 1 match (semantic heading)
- TypeScript compiles without errors
</acceptance_criteria>
</task>

---

## Task 5: Contact section component (HOME-06)

<task id="04-02-T5">
<read_first>
- .planning/phases/04-homepage/04-UI-SPEC.md (Layout Contract → Contact Section)
- .planning/phases/04-homepage/04-UI-SPEC.md (Interaction States → Email link, Text link)
- .planning/phases/04-homepage/04-UI-SPEC.md (Copywriting Contract → Contact copy EN + IT)
- src/components/ui/Tag.tsx (Tag component for availability tag)
- src/components/sections/SectionHeader.tsx (SectionHeader API)
- src/components/layout/Section.tsx (Section wrapper — default 720px)
</read_first>

**Action:** Create `src/components/sections/Contact.tsx` as a Server Component. This component may be reused on inner pages (Phase 5).

**Props interface:**
```typescript
interface ContactProps {
  label: string;
  title: string;
  text: string;
  email: string;
  linkedinLabel: string;
  linkedinHref: string;
  cvLabel: string;
  cvHref: string;
  availability: string;
}
```

**Layout:**
- Use `Section` wrapper (default 720px) with `id="contact"` for anchor link from Hero CTA
- `SectionHeader` with label + title
- Invite text: `<p>`, body 16px, leading-[1.7], text-secondary, mb-8
- Email link: `<a href="mailto:{email}">`, font-heading (Space Grotesk) font-bold, font-mono for the email itself, text-[clamp(28px,3vw,36px)], text-primary, hover:text-accent, transition-colors duration-150. Display as block element.
  - NOTE: UI-SPEC says "title size, JetBrains Mono" but also "Space Grotesk 700". The email uses JetBrains Mono (monospace) at title size. Use `font-mono text-[clamp(28px,3vw,36px)] font-bold`.
- Links group (LinkedIn, CV): mt-6, flex flex-col gap-4 or inline
  - Each: `<a>`, body size 16px, text-secondary, hover:text-accent, transition-colors duration-150
  - LinkedIn: `href="https://linkedin.com/in/selfrules"`, target="_blank", rel="noopener noreferrer"
  - CV: `href="/mattia-de-luca-cv.pdf"` (or appropriate path), download attribute
  - Arrow suffix: ` →` appended to link text
- Availability tag: mt-8, use `Tag` component with label prop, or render inline as monospace text
  - `<p>`, font-mono, text-sm, uppercase, tracking-[0.05em], text-secondary

<acceptance_criteria>
- File exists at src/components/sections/Contact.tsx
- grep `"use client"` src/components/sections/Contact.tsx returns 0 matches (Server Component)
- grep `export function Contact` src/components/sections/Contact.tsx returns 1 match
- grep `SectionHeader` src/components/sections/Contact.tsx returns at least 1 match
- grep `mailto:` src/components/sections/Contact.tsx returns at least 1 match (email link)
- grep `font-mono` src/components/sections/Contact.tsx returns at least 1 match (email monospace)
- grep `hover:text-accent` src/components/sections/Contact.tsx returns at least 1 match (hover state)
- grep `id="contact"\|id=.contact.` src/components/sections/Contact.tsx returns at least 1 match (anchor target for Hero CTA)
- grep `linkedin` src/components/sections/Contact.tsx returns at least 1 match (LinkedIn link)
- grep `availability` src/components/sections/Contact.tsx returns at least 1 match
- grep `target="_blank"` src/components/sections/Contact.tsx returns at least 1 match (external link)
- TypeScript compiles without errors
</acceptance_criteria>
</task>
