---
wave: 1
depends_on: ["03-02-PLAN"]
files_modified:
  - src/i18n/messages/en.json
  - src/i18n/messages/it.json
  - src/components/sections/SectionHeader.tsx
  - src/components/sections/Hero.tsx
autonomous: true
requirements: [HOME-01]
---

# Plan 04-01: i18n Homepage Namespace + SectionHeader + Hero Section

## Objective

Populate the i18n JSON files with the complete homepage namespace (all 6 sections, EN + IT), create the shared SectionHeader component used by 5 of 6 sections, and build the Hero section component with terminal prompt, blinking cursor, credential tags, separator, and signature phrase. After this plan, the Hero renders at `/` and `/it` with full bilingual content inside a 100vh viewport.

---

## Task 1: Populate homepage i18n namespace (EN)

<task id="04-01-T1">
<read_first>
- src/i18n/messages/en.json (current structure)
- ../job-search-2026/selfrules-redesign/homepage.md (EN section — lines 130-248, COPY VERBATIM)
- ../job-search-2026/selfrules-redesign/microcopy.md (EN CTAs and labels)
- .planning/phases/04-homepage/04-UI-SPEC.md (Copywriting Contract EN table)
</read_first>

**Action:** Add a `homepage` top-level key to `en.json` with deeply nested keys for all 6 sections. Use the namespace structure from 04-RESEARCH.md Pattern 4. Every string must be copied VERBATIM from `homepage.md` EN section. For strings containing bold markers (e.g. `**25%**`), use `<b>` tags instead (next-intl rich text format): `<b>25%</b>`.

**Concrete values for the `homepage` namespace:**

```
homepage.hero.headline = "Senior Technical Product Manager. Design, code, and product management in the same head."
homepage.hero.subtitle = "B2B SaaS . Payments . Platform. 10+ years designing interfaces, writing production code, and managing products. The advantage: when design, engineering, and business don't understand each other, no translator needed."
homepage.hero.primaryCta = "Get in touch"
homepage.hero.secondaryCta = "See the work"
homepage.hero.tag1 = "10+ years"
homepage.hero.tag2 = "B2B SaaS"
homepage.hero.tag3 = "Payments"
homepage.hero.tag4 = "5 countries"
homepage.hero.terminalPrompt = "mattia@selfrules ~ $"
homepage.hero.signaturePhrase = "The problem is never the one from the first meeting."

homepage.howIWork.label = "HOW I WORK"
homepage.howIWork.title = "How I work"
homepage.howIWork.intro = "When design says \"the user gets stuck here because the visual hierarchy is wrong,\" engineering says \"we need 3 sprints of refactoring,\" and business says \"it's costing us €15K a month in support tickets.\" Three separate conversations. My job is to turn them into one decision."
homepage.howIWork.pillar1.title = "Designer + Developer + PM. In the same room."
homepage.howIWork.pillar1.text = "4 years designing interfaces. 4 writing production code. 5+ managing products and cross-functional teams of up to 10+ people. When design and engineering don't understand each other, the problem usually isn't technical. It's that nobody in the room has done both jobs. Weekly cross-functional sessions cut post-release incidents by <b>25%</b>. Less rework, fewer meetings, decisions that stick."
homepage.howIWork.pillar2.title = "The problem is never the one from the first meeting"
homepage.howIWork.pillar2.text = "An example. Payment system in crisis, 44 open problems. The pressure is \"act on everything, now.\" Two weeks talking to support, reading tickets, looking at the data. Result: 12 critical, 4 with immediate financial impact. The rest could wait. The 4 most severe resolved in 6 weeks, not 6 months. Before building anything, verify you're solving the right problem."
homepage.howIWork.pillar3.title = "Prototypes, not slides"
homepage.howIWork.pillar3.text = "A React prototype users can touch says more than any spec document. React, TypeScript, and Node.js to validate an idea before asking the team for months of development. Priority comes from user frustration, not technical complexity. And when the data says a feature isn't needed, the answer is no. Even when 60% of users are asking for it."

homepage.timeline.label = "THE PATH"
homepage.timeline.title = "The path"
homepage.timeline.block1.role = "Founder & Designer"
homepage.timeline.block1.dates = "2012-2018"
homepage.timeline.block1.text = "A web design studio launched at 25 in northern Italy. 20+ clients, everything in one person: sales, design, development, delivery, support. First e-commerce site: award-worthy interface, zero sales. The rule that came out of it still holds."
homepage.timeline.block2.role = "Designer & Developer"
homepage.timeline.block2.dates = "2016-2020"
homepage.timeline.block2.text = "Joined as a UX Designer, left writing production code. B2B and B2C products for clients including Prometeia, Casavo, CliensPiu. CliensPiu: a legal practice management system used 8+ hours a day, designed and built by the same person. After 4 years of designing and then implementing the same product, the line between the two disciplines stopped making sense."
homepage.timeline.block3.role = "Product Owner"
homepage.timeline.block3.dates = "2020-2023"
homepage.timeline.block3.text = "LeadsBridge: B2B SaaS platform with 380+ connectors, Meta, Google, LinkedIn, TikTok partnerships. Solo PM, fully remote from Europe. The core flow redesign cut setup time by <b>35%</b> and unlocked revenue growth."
homepage.timeline.block4.role = "Product Manager"
homepage.timeline.block4.dates = "2023-present"
homepage.timeline.block4.text = "Payment systems, platform integrations, and cashless solutions for centers across 5 countries. A system inherited in critical state. Today: <b>99%+</b> uptime, <b>-25%</b> post-release incidents, <b>116 centers</b> integrated, team of 10+."
homepage.timeline.closingLine = "Today when I talk to a designer, I understand the constraints. When I talk to a developer, I understand the trade-offs. When I talk to business, I translate."

homepage.metrics.label = "NUMBERS, NOT WORDS"
homepage.metrics.title = "Numbers, not words"
homepage.metrics.metric1.number = "99%+"
homepage.metrics.metric1.context = "Payment uptime and success rate — QubicaAMF. From daily crises to enterprise stability."
homepage.metrics.metric2.number = "-25%"
homepage.metrics.metric2.context = "Post-release incidents — QubicaAMF. Weekly cross-functional sessions with Product, Support, and Engineering."
homepage.metrics.metric3.number = "116"
homepage.metrics.metric3.context = "Centers integrated on the platform — QubicaAMF. 5 new per week."
homepage.metrics.metric4.number = "-35%"
homepage.metrics.metric4.context = "Setup time — LeadsBridge. Core product flow redesign."
homepage.metrics.metric5.number = "+9%"
homepage.metrics.metric5.context = "Integration adoption — QubicaAMF. 47-page PDFs replaced with 3-minute videos."
homepage.metrics.metric6.number = "-12%"
homepage.metrics.metric6.context = "Payment processing times — QubicaAMF. Full payment pipeline optimization."
homepage.metrics.link = "Read the full case studies"

homepage.currentWork.label = "WHAT I'M DOING NOW"
homepage.currentWork.title = "What I'm doing now"
homepage.currentWork.card1.title = "Payments and platform at international scale"
homepage.currentWork.card1.text = "Payment systems, platform integrations, and cashless solutions for centers across 5 countries. Next step: bringing AI into the operational flow to reduce manual reconciliation and support workload."
homepage.currentWork.card2.title = "A personal problem turned product"
homepage.currentWork.card2.text = "Finding an apartment across 8 listing sites by hand wasn't working. Automated scraping, smart scoring, 880+ listings analyzed. The tool is still running."

homepage.contact.label = "LET'S TALK"
homepage.contact.title = "Let's talk"
homepage.contact.text = "If you think my profile could work for your team, write me. Even if you're not sure it's the right fit. The best conversations I've had started with \"I'm not sure this is your thing, but...\""
homepage.contact.email = "mattia@selfrules.org"
homepage.contact.linkedinLabel = "LinkedIn"
homepage.contact.cvLabel = "Download my CV (PDF)"
homepage.contact.availability = "EU remote . US remote . Italy hybrid"
```

<acceptance_criteria>
- grep `"homepage"` src/i18n/messages/en.json returns at least 1 match
- grep `"hero"` src/i18n/messages/en.json returns at least 1 match
- grep `"Senior Technical Product Manager. Design, code, and product management in the same head."` src/i18n/messages/en.json returns exactly 1 match
- grep `"howIWork"` src/i18n/messages/en.json returns at least 1 match
- grep `"timeline"` src/i18n/messages/en.json returns at least 1 match
- grep `"metrics"` src/i18n/messages/en.json returns at least 1 match
- grep `"currentWork"` src/i18n/messages/en.json returns at least 1 match
- grep `"contact"` src/i18n/messages/en.json returns at least 1 match
- grep `"99%+"` src/i18n/messages/en.json returns at least 1 match
- grep `"signaturePhrase"` src/i18n/messages/en.json returns 1 match
- grep `"<b>"` src/i18n/messages/en.json returns at least 4 matches (bold numbers in pillar/timeline copy)
- `npx next build` compiles without errors related to missing i18n keys
</acceptance_criteria>
</task>

---

## Task 2: Populate homepage i18n namespace (IT)

<task id="04-01-T2">
<read_first>
- src/i18n/messages/it.json (current structure)
- ../job-search-2026/selfrules-redesign/homepage.md (IT section — lines 1-127, COPY VERBATIM)
- ../job-search-2026/selfrules-redesign/microcopy.md (IT CTAs and labels)
- .planning/phases/04-homepage/04-UI-SPEC.md (Copywriting Contract IT table)
</read_first>

**Action:** Add the same `homepage` top-level key to `it.json` with identical structure to EN but with Italian copy from `homepage.md` IT section. Every string must be copied VERBATIM. Use `<b>` tags for bold markers.

**Concrete values for the `homepage` namespace:**

```
homepage.hero.headline = "Senior Technical Product Manager. Design, codice, e product management nella stessa testa."
homepage.hero.subtitle = "B2B SaaS . Payments . Platform. 10+ anni a progettare interfacce, scrivere codice in produzione, e gestire prodotti. Il vantaggio: quando design, engineering e business non si capiscono, non serve un interprete."
homepage.hero.primaryCta = "Scrivimi"
homepage.hero.secondaryCta = "Guarda i lavori"
homepage.hero.tag1 = "10+ anni"
homepage.hero.tag2 = "B2B SaaS"
homepage.hero.tag3 = "Payments"
homepage.hero.tag4 = "5 paesi"
homepage.hero.terminalPrompt = "mattia@selfrules ~ $"
homepage.hero.signaturePhrase = "Il problema non e' mai quello del primo meeting."

homepage.howIWork.label = "COME LAVORO"
homepage.howIWork.title = "Come lavoro"
homepage.howIWork.intro = (full IT intro text from homepage.md)
homepage.howIWork.pillar1.title = "Designer + Developer + PM. Nella stessa stanza."
homepage.howIWork.pillar1.text = (full IT text with <b>25%</b>)
homepage.howIWork.pillar2.title = "Il problema non e' mai quello del primo meeting"
homepage.howIWork.pillar2.text = (full IT text)
homepage.howIWork.pillar3.title = "Prototipi, non slide"
homepage.howIWork.pillar3.text = (full IT text)

homepage.timeline.label = "IL PERCORSO"
homepage.timeline.title = "Il percorso"
(all 4 blocks + closingLine from IT copy)

homepage.metrics.label = "NUMERI, NON PAROLE"
homepage.metrics.title = "Numeri, non parole"
(all 6 metrics from IT copy)
homepage.metrics.link = "Leggi i case study completi"

homepage.currentWork.label = "COSA FACCIO ORA"
homepage.currentWork.title = "Cosa faccio ora"
(both cards from IT copy)

homepage.contact.label = "PARLIAMO"
homepage.contact.title = "Parliamo"
homepage.contact.text = (full IT contact text)
homepage.contact.email = "mattia@selfrules.org"
homepage.contact.linkedinLabel = "LinkedIn"
homepage.contact.cvLabel = "Scarica il mio CV (PDF)"
homepage.contact.availability = "EU remote . US remote . Italia ibrido"
```

<acceptance_criteria>
- grep `"homepage"` src/i18n/messages/it.json returns at least 1 match
- grep `"Senior Technical Product Manager. Design, codice, e product management nella stessa testa."` src/i18n/messages/it.json returns exactly 1 match
- grep `"Come lavoro"` src/i18n/messages/it.json returns at least 1 match
- grep `"Il percorso"` src/i18n/messages/it.json returns at least 1 match
- grep `"Numeri, non parole"` src/i18n/messages/it.json returns at least 1 match
- grep `"Cosa faccio ora"` src/i18n/messages/it.json returns at least 1 match
- grep `"Parliamo"` src/i18n/messages/it.json returns at least 1 match
- grep `"signaturePhrase"` src/i18n/messages/it.json returns 1 match
- grep `"Scarica il mio CV"` src/i18n/messages/it.json returns 1 match
- The key structure in it.json exactly mirrors en.json (same keys, different values)
</acceptance_criteria>
</task>

---

## Task 3: Create SectionHeader shared component

<task id="04-01-T3">
<read_first>
- .planning/phases/04-homepage/04-UI-SPEC.md (Typography table — Label role: 14px JetBrains Mono, uppercase, 0.05em; Title role: Space Grotesk 700, clamp 28-36px)
- .planning/phases/04-homepage/04-CONTEXT.md (D-03: shared SectionHeader)
- .planning/phases/04-homepage/04-RESEARCH.md (Pattern 1: SectionHeader)
- src/components/ui/ (check existing component naming conventions)
</read_first>

**Action:** Create `src/components/sections/SectionHeader.tsx` as a Server Component (no 'use client'). It renders a label (monospace uppercase) and a title (Space Grotesk bold).

**Props interface:**
```typescript
interface SectionHeaderProps {
  label: string;
  title: string;
}
```

**Implementation details:**
- Label: `<p>` element, classes `font-mono text-sm uppercase tracking-[0.05em] text-secondary mb-3`, font-weight 400 (label role uses font-family distinction, not weight)
- Title: `<h2>` element, classes `font-heading text-[clamp(28px,3vw,36px)] font-bold leading-[1.2] text-primary`
- Wrapper: `<div className="mb-12">`
- NO 'use client' directive
- Export as named export: `export function SectionHeader`

<acceptance_criteria>
- File exists at src/components/sections/SectionHeader.tsx
- grep `"use client"` src/components/sections/SectionHeader.tsx returns 0 matches (Server Component)
- grep `font-heading` src/components/sections/SectionHeader.tsx returns at least 1 match
- grep `font-mono` src/components/sections/SectionHeader.tsx returns at least 1 match
- grep `uppercase` src/components/sections/SectionHeader.tsx returns at least 1 match
- grep `tracking-\[0.05em\]` src/components/sections/SectionHeader.tsx returns at least 1 match
- grep `clamp(28px,3vw,36px)` src/components/sections/SectionHeader.tsx returns at least 1 match
- grep `font-bold` src/components/sections/SectionHeader.tsx returns at least 1 match
- grep `export function SectionHeader` src/components/sections/SectionHeader.tsx returns 1 match
- grep `<h2` src/components/sections/SectionHeader.tsx returns at least 1 match (semantic heading)
- TypeScript compiles without errors
</acceptance_criteria>
</task>

---

## Task 4: Create Hero section component

<task id="04-01-T4">
<read_first>
- .planning/phases/04-homepage/04-UI-SPEC.md (Layout Contract → Hero Section, full spec)
- .planning/phases/04-homepage/04-CONTEXT.md (D-01: upper third positioning, D-02: no scroll cue)
- .planning/phases/04-homepage/04-RESEARCH.md (Code Example: Hero Upper-Third Positioning)
- src/components/ui/Button.tsx (existing Button API: variant, href props)
- src/components/ui/Tag.tsx (existing Tag API: label prop)
- src/components/ui/TerminalPrompt.tsx (existing component from Phase 3 — if not present, will need to be created in this task as part of Hero)
- src/components/ui/BlinkingCursor.tsx (existing component from Phase 3 — if not present, will need to be created in this task)
</read_first>

**Action:** Create `src/components/sections/Hero.tsx` as a Server Component. The Hero fills 100vh, positions content in the upper portion (NOT vertically centered), and renders all 7 hero elements in order.

**Props interface:**
```typescript
interface HeroProps {
  headline: string;
  subtitle: string;
  primaryCta: string;
  secondaryCta: string;
  tags: string[];
  terminalPrompt: string;
  signaturePhrase: string;
}
```

**Layout (top to bottom, per UI-SPEC):**
1. TerminalPrompt — `mattia@selfrules ~ $` in JetBrains Mono 14px, color text-tertiary (#5A5A5E), with amber block cursor. `aria-hidden="true"` (decorative). Margin-bottom 24px.
2. Headline — `<h1>`, font-heading (Space Grotesk), Display size clamp(40px,5vw,64px), font-bold, leading-[1.1], text-primary.
3. Subtitle — `<p>`, font-sans (Inter), 16px, leading-[1.7], text-secondary, max-w-[600px]. At end of text, render BlinkingCursor type="underscore" in accent color. Margin-top 16px.
4. CTA buttons — Two Button components side by side, 16px gap. Primary variant links to `#contact`, secondary links to `/work`. Margin-top 32px.
5. Credential tags — Row of Tag components, 8px gap. Margin-top 24px.
6. Separator line — `<div>` with h-px, w-[200px], bg-border (border-default #1A1A1F). Margin-top 32px.
7. Signature phrase — `<p>`, italic, text-sm (14-16px), text-secondary. Margin-top 16px.
8. Empty space — remaining viewport height (hero fills 100vh).

**Container structure:**
- Outer: `<section aria-label="Hero" className="min-h-screen flex flex-col justify-start pt-[28vh] px-[clamp(20px,5vw,80px)]">`
- Inner: `<div className="max-w-[720px]">`

**Dependencies on Phase 3 components:**
- If `TerminalPrompt` and `BlinkingCursor` components exist from Phase 3, import them.
- If they do NOT exist yet (Phase 3 not executed), create minimal inline versions:
  - TerminalPrompt: `<span aria-hidden="true" className="font-mono text-sm text-tertiary">` + prompt text + amber block cursor span
  - BlinkingCursor: a `'use client'` component with CSS keyframe animation (`opacity 0<->1, step-end, 1s infinite`), respecting `prefers-reduced-motion`

**Critical constraints:**
- min-h-screen is NON-NEGOTIABLE — hero MUST fill entire viewport at first load
- Content in UPPER portion, NOT vertically centered (pt-[28vh] not items-center)
- 0px border-radius on everything
- Only font-weights 400 and 700

<acceptance_criteria>
- File exists at src/components/sections/Hero.tsx
- grep `min-h-screen` src/components/sections/Hero.tsx returns at least 1 match (100vh enforcement)
- grep `pt-\[28vh\]` src/components/sections/Hero.tsx returns at least 1 match OR grep `pt-\[` returns a value between 25-32vh (upper third positioning)
- grep `items-center` src/components/sections/Hero.tsx returns 0 matches (NOT vertically centered)
- grep `<h1` src/components/sections/Hero.tsx returns at least 1 match (semantic H1)
- grep `font-heading` src/components/sections/Hero.tsx returns at least 1 match (Space Grotesk for headline)
- grep `clamp(40px,5vw,64px)` src/components/sections/Hero.tsx returns at least 1 match (Display size)
- grep `max-w-\[720px\]` src/components/sections/Hero.tsx returns at least 1 match (content width)
- grep `aria-hidden` src/components/sections/Hero.tsx returns at least 1 match (terminal prompt accessibility)
- grep `signaturePhrase` src/components/sections/Hero.tsx returns at least 1 match
- grep `italic` src/components/sections/Hero.tsx returns at least 1 match (signature phrase styling)
- grep `w-\[200px\]` src/components/sections/Hero.tsx returns at least 1 match (separator line width)
- grep `export function Hero` src/components/sections/Hero.tsx returns 1 match
- grep `#contact` src/components/sections/Hero.tsx returns at least 1 match (primary CTA target)
- TypeScript compiles without errors
- `npx next build` succeeds if Hero is wired into page.tsx
</acceptance_criteria>
</task>

---

## Task 5: Wire Hero into page.tsx for verification

<task id="04-01-T5">
<read_first>
- src/app/[locale]/page.tsx (current homepage file)
- .planning/phases/04-homepage/04-RESEARCH.md (Pattern 3: page.tsx as translation orchestrator)
- .planning/phases/04-homepage/04-RESEARCH.md (Pitfall 1: setRequestLocale, Pitfall 7: params as Promise)
</read_first>

**Action:** Update `src/app/[locale]/page.tsx` to import and render the Hero section with translated props. This is a partial wiring — remaining sections will be added in Plan 04-03.

**Implementation:**
- Import `Hero` from `@/components/sections/Hero`
- Import `setRequestLocale`, `getTranslations` from `next-intl/server`
- `const { locale } = await params;` (params is Promise in Next.js 16)
- `setRequestLocale(locale);` MUST be first call (prevents dynamic rendering)
- `const t = await getTranslations('homepage');`
- Render `<Hero>` with all props mapped from `t('hero.headline')`, `t('hero.subtitle')`, etc.
- Tags array: `[t('hero.tag1'), t('hero.tag2'), t('hero.tag3'), t('hero.tag4')]`
- Keep `generateStaticParams` returning `[{ locale: 'en' }, { locale: 'it' }]`

<acceptance_criteria>
- grep `setRequestLocale` src/app/[locale]/page.tsx returns at least 1 match
- grep `getTranslations` src/app/[locale]/page.tsx returns at least 1 match
- grep `await params` src/app/[locale]/page.tsx returns at least 1 match (Next.js 16 Promise pattern)
- grep `Hero` src/app/[locale]/page.tsx returns at least 1 match
- grep `generateStaticParams` src/app/[locale]/page.tsx returns at least 1 match
- `npx next build` succeeds with static output (circle icon, not lambda) for both / and /it routes
- Visiting localhost:3000 shows the Hero section with EN content
- Visiting localhost:3000/it shows the Hero section with IT content
</acceptance_criteria>
</task>
