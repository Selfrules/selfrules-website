---
phase: 04-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/messages/en.json
  - src/i18n/messages/it.json
  - src/components/sections/section-header.tsx
  - src/components/sections/hero.tsx
autonomous: true
requirements: [HOME-01]

must_haves:
  truths:
    - "Homepage i18n namespace contains all ~200 keys for 6 sections in both EN and IT"
    - "SectionHeader renders monospace uppercase label + bold title consistently"
    - "Hero section fills viewport with upper-third positioned content, 2 CTAs, and credential tags"
  artifacts:
    - path: "src/i18n/messages/en.json"
      provides: "Full homepage.* namespace with all section keys in English"
      contains: "homepage"
    - path: "src/i18n/messages/it.json"
      provides: "Full homepage.* namespace with all section keys in Italian"
      contains: "homepage"
    - path: "src/components/sections/section-header.tsx"
      provides: "Shared label+title component for 5 sections"
      exports: ["SectionHeader"]
    - path: "src/components/sections/hero.tsx"
      provides: "Full-viewport hero section with upper-third positioning"
      exports: ["Hero"]
  key_links:
    - from: "src/components/sections/hero.tsx"
      to: "src/components/ui/button.tsx"
      via: "import Button"
      pattern: "import.*Button.*from.*ui/button"
    - from: "src/components/sections/hero.tsx"
      to: "src/components/ui/tag.tsx"
      via: "import Tag"
      pattern: "import.*Tag.*from.*ui/tag"
---

<objective>
Populate the full homepage i18n namespace (both locales), create the shared SectionHeader component, and build the Hero section -- establishing the translation pattern, component architecture, and visual language that the remaining 5 sections will follow.

Purpose: Hero is the first thing visitors see and sets the editorial tone. The i18n JSON files are the foundation that every section component consumes. SectionHeader (D-03) ensures visual consistency across all sections.
Output: Complete i18n content for entire homepage, SectionHeader component, Hero section component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage/04-CONTEXT.md
@.planning/phases/04-homepage/04-RESEARCH.md
@.planning/phases/04-homepage/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate full homepage i18n namespace (EN + IT) and create SectionHeader component</name>
  <files>src/i18n/messages/en.json, src/i18n/messages/it.json, src/components/sections/section-header.tsx</files>
  <read_first>
    - src/i18n/messages/en.json (current state of i18n file)
    - src/i18n/messages/it.json (current state of i18n file)
    - ../job-search-2026/selfrules-redesign/homepage.md (source copy -- COPY VERBATIM)
    - ../job-search-2026/selfrules-redesign/microcopy.md (CTA labels, availability tag)
    - .planning/phases/04-homepage/04-UI-SPEC.md (typography roles for SectionHeader)
    - .planning/phases/04-homepage/04-CONTEXT.md (D-06, D-07 namespace decisions)
  </read_first>
  <action>
**Part A: i18n JSON files**

Add `homepage` namespace to both `en.json` and `it.json`. Per D-06 (deep namespace) and D-07 (numbered keys, not arrays).

Structure:
```json
{
  "homepage": {
    "hero": {
      "headline": "...",
      "subtitle": "...",
      "primaryCta": "...",
      "secondaryCta": "...",
      "tag1": "...",
      "tag2": "...",
      "tag3": "...",
      "tag4": "..."
    },
    "howIWork": {
      "label": "...",
      "title": "...",
      "intro": "...",
      "pillar1": { "title": "...", "text": "..." },
      "pillar2": { "title": "...", "text": "..." },
      "pillar3": { "title": "...", "text": "..." }
    },
    "timeline": {
      "label": "...",
      "title": "...",
      "block1": { "role": "...", "company": "...", "dates": "...", "text": "..." },
      "block2": { "role": "...", "company": "...", "dates": "...", "text": "..." },
      "block3": { "role": "...", "company": "...", "dates": "...", "text": "..." },
      "block4": { "role": "...", "company": "...", "dates": "...", "text": "..." },
      "closingLine": "..."
    },
    "metrics": {
      "label": "...",
      "title": "...",
      "metric1": { "number": "99%+", "context": "..." },
      "metric2": { "number": "-25%", "context": "..." },
      "metric3": { "number": "116", "context": "..." },
      "metric4": { "number": "-35%", "context": "..." },
      "metric5": { "number": "+9%", "context": "..." },
      "metric6": { "number": "-12%", "context": "..." },
      "link": "..."
    },
    "currentWork": {
      "label": "...",
      "title": "...",
      "card1": { "title": "...", "text": "..." },
      "card2": { "title": "...", "text": "..." }
    },
    "contact": {
      "label": "...",
      "title": "...",
      "text": "...",
      "email": "mattia@selfrules.org",
      "linkedinLabel": "LinkedIn",
      "cvLabel": "...",
      "availability": "..."
    }
  }
}
```

EN copy values (from homepage.md ENGLISH section):
- `hero.headline`: `"Senior Technical Product Manager. Design, code, and product management in the same head."`
- `hero.subtitle`: `"B2B SaaS \u00b7 Payments \u00b7 Platform. 10+ years designing interfaces, writing production code, and managing products. The advantage: when design, engineering, and business don't understand each other, no translator needed."`
- `hero.primaryCta`: `"Get in touch"`
- `hero.secondaryCta`: `"See the work"`
- `hero.tag1`: `"10+ years"`, `hero.tag2`: `"B2B SaaS"`, `hero.tag3`: `"Payments"`, `hero.tag4`: `"5 countries"`
- `howIWork.label`: `"How I work"`, `howIWork.title`: `"How I work"`
- `howIWork.intro`: copy verbatim from homepage.md Section 2 intro
- `howIWork.pillar1.title`: `"Designer + Developer + PM. In the same room."`
- `howIWork.pillar1.text`: copy verbatim (use `<b>25%</b>` for bold numbers -- next-intl rich text)
- `howIWork.pillar2.title`: `"The problem is never the one from the first meeting"`
- `howIWork.pillar2.text`: copy verbatim
- `howIWork.pillar3.title`: `"Prototypes, not slides"`
- `howIWork.pillar3.text`: copy verbatim
- `timeline.label`: `"The path"`, `timeline.title`: `"The path"`
- Each block: `role`, `company` (extract from title e.g. "Founder & Designer"), `dates` (e.g. "2012-2018"), `text` (copy verbatim, use `<b>` for bold numbers)
- `timeline.block1.company`: `""` (own studio), `timeline.block2.company`: `""` (agency), `timeline.block3.company`: `"LeadsBridge"`, `timeline.block4.company`: `"QubicaAMF"`
- `timeline.closingLine`: copy verbatim from homepage.md
- `metrics.label`: `"Numbers, not words"`, `metrics.title`: `"Numbers, not words"`
- Each metric: `number` and `context` from homepage.md verbatim
- `metrics.link`: `"Read the full case studies"`
- `currentWork.label`: `"What I'm doing now"`, `currentWork.title`: `"What I'm doing now"`
- Each card: `title` and `text` from homepage.md verbatim
- `contact.label`: `"Let's talk"`, `contact.title`: `"Let's talk"`
- `contact.text`: copy verbatim from homepage.md
- `contact.email`: `"mattia@selfrules.org"`, `contact.linkedinLabel`: `"LinkedIn"`
- `contact.cvLabel`: `"Download my CV (PDF)"`, `contact.availability`: `"EU remote \u00b7 US remote \u00b7 Italy hybrid"`

IT copy: same structure, all values from homepage.md ITALIANO section. Use `\u00b7` for middle dots. Use `<b>` tags for bold numbers in text.

CRITICAL: Copy every string EXACTLY from the source files. No paraphrasing, no "improving", no summarizing.

**Part B: SectionHeader component**

Create `src/components/sections/section-header.tsx` per D-03:

```typescript
interface SectionHeaderProps {
  label: string;
  title: string;
}

export function SectionHeader({ label, title }: SectionHeaderProps) {
  return (
    <div className="mb-12">
      <p className="font-mono text-sm uppercase tracking-[0.05em] text-secondary mb-3">
        {label}
      </p>
      <h2 className="text-[clamp(28px,3vw,36px)] font-bold leading-[1.2] text-primary">
        {title}
      </h2>
    </div>
  );
}
```

Typography from UI-SPEC:
- Label: 14px (text-sm), JetBrains Mono (font-mono), weight 400 (implicit), uppercase, letter-spacing 0.05em, text-secondary
- Title: clamp(28px,3vw,36px), weight 700 (font-bold), leading 1.2, text-primary
  </action>
  <verify>
    <automated>grep -c '"homepage"' src/i18n/messages/en.json && grep -c '"homepage"' src/i18n/messages/it.json && grep -c 'SectionHeader' src/components/sections/section-header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains key "homepage" with nested keys: hero, howIWork, timeline, metrics, currentWork, contact
    - it.json contains same structure with Italian translations
    - en.json contains `"hero.headline"` or nested `"headline"` under `"hero"` matching "Senior Technical Product Manager. Design, code, and product management in the same head."
    - it.json hero.headline matches "Senior Technical Product Manager. Design, codice, e product management nella stessa testa."
    - en.json contains 6 metric entries (metric1 through metric6) each with "number" and "context"
    - Bold numbers in pillar/timeline text use `<b>` tags (e.g., `<b>25%</b>`)
    - section-header.tsx exports `SectionHeader` function component
    - section-header.tsx contains `font-mono`, `text-sm`, `uppercase`, `tracking-[0.05em]`, `text-secondary`
    - section-header.tsx contains `text-[clamp(28px,3vw,36px)]`, `font-bold`, `leading-[1.2]`
    - section-header.tsx renders `<h2>` for the title (correct heading hierarchy)
  </acceptance_criteria>
  <done>Both i18n JSON files contain the complete homepage namespace (~200 keys total, all 6 sections) with copy identical to source files. SectionHeader component exports and renders label+title with correct typography.</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero section component (HOME-01)</name>
  <files>src/components/sections/hero.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (Button API -- variant prop, href prop)
    - src/components/ui/tag.tsx (Tag API -- label prop)
    - src/i18n/messages/en.json (verify hero keys exist from Task 1)
    - .planning/phases/04-homepage/04-UI-SPEC.md (Hero layout contract)
    - .planning/phases/04-homepage/04-CONTEXT.md (D-01 upper third, D-02 no scroll cue, D-05 Server Component)
    - ../job-search-2026/selfrules-redesign/homepage.md (verify copy accuracy)
  </read_first>
  <action>
Create `src/components/sections/hero.tsx` -- a Server Component (no 'use client' -- per D-05).

Props interface:
```typescript
interface HeroProps {
  headline: string;
  subtitle: string;
  primaryCta: string;
  primaryCtaHref: string;
  secondaryCta: string;
  secondaryCtaHref: string;
  tags: string[];
}
```

Layout per D-01 and UI-SPEC Hero section contract:
- Outer: `<section className="min-h-screen flex flex-col justify-start pt-[28vh] px-[clamp(20px,5vw,80px)]">`
- Content wrapper: `<div className="max-w-[720px]">`
- H1 (Display role): `className="text-[clamp(40px,5vw,64px)] font-bold leading-[1.1] text-primary"`
- Subtitle (Body role, elevated line-height): `className="mt-6 text-base leading-[1.7] text-secondary max-w-[600px]"`
- CTA row: `className="mt-8 flex gap-4"` with `<Button variant="primary" href={primaryCtaHref}>{primaryCta}</Button>` and `<Button variant="secondary" href={secondaryCtaHref}>{secondaryCta}</Button>`
- Tags row: `className="mt-6 flex flex-wrap gap-2"` mapping over `tags` rendering `<Tag key={i} label={tag} />`

Per D-02: NO scroll cue indicator (no arrow, no gradient, no "scroll down" text).

Import Button from `@/components/ui/button` and Tag from `@/components/ui/tag`.

The H1 is the ONLY h1 on the page (correct heading hierarchy per accessibility contract).
  </action>
  <verify>
    <automated>grep -c 'export function Hero' src/components/sections/hero.tsx && grep -c 'min-h-screen' src/components/sections/hero.tsx && grep -c 'pt-\[28vh\]' src/components/sections/hero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - hero.tsx exports `Hero` function component
    - hero.tsx does NOT contain 'use client' (Server Component per D-05)
    - hero.tsx contains `min-h-screen` (full viewport height)
    - hero.tsx contains `pt-[28vh]` or similar upper-third positioning (NOT `items-center` or `justify-center`)
    - hero.tsx contains `text-[clamp(40px,5vw,64px)]` for Display typography
    - hero.tsx contains `font-bold` and `leading-[1.1]` on the h1
    - hero.tsx renders `<h1>` element for the headline
    - hero.tsx imports Button from `@/components/ui/button`
    - hero.tsx imports Tag from `@/components/ui/tag`
    - hero.tsx contains `max-w-[720px]` for content width constraint
    - hero.tsx does NOT contain any scroll indicator/arrow/cue (per D-02)
    - hero.tsx renders both primary and secondary CTA buttons
    - hero.tsx maps over tags array to render Tag components
  </acceptance_criteria>
  <done>Hero section component renders full-viewport section with upper-third positioning, headline (h1), subtitle, 2 CTA buttons (primary + secondary), and credential tags row. No scroll cue. Server Component only.</done>
</task>

</tasks>

<verification>
- `npx next build` compiles without errors (type-check + static generation)
- Both i18n JSON files are valid JSON and contain the `homepage` namespace
- SectionHeader and Hero components export correctly
- Hero uses correct typography roles from UI-SPEC (Display for h1, Body for subtitle, Label for tags)
- No 'use client' directive in any section component
</verification>

<success_criteria>
- Complete i18n content for all 6 homepage sections exists in both en.json and it.json
- SectionHeader renders label (mono, uppercase, secondary) + title (clamp, bold) consistently
- Hero fills viewport with upper-third content placement, 2 CTAs, and credential tags
- All copy matches source files exactly
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage/04-01-SUMMARY.md`
</output>
