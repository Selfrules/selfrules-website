---
phase: 06-seo-polish-deploy
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/app/[locale]/layout.tsx
  - src/app/globals.css
  - next.config.ts
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "next build succeeds and reports first load JS < 100KB for all routes"
    - "No build warnings or errors related to bundle size"
    - "All pages are statically rendered (circle icon in build output, not lambda)"
    - "LCP < 2.5s and CLS < 0.1 verified via Lighthouse audit (PERF-06)"
  artifacts:
    - path: "next.config.ts"
      provides: "Optimized Next.js config for production performance"
  key_links:
    - from: "npx next build"
      to: "all page files"
      via: "build output bundle analysis"
      pattern: "First Load JS"
---

<objective>
Verify and optimize site performance to meet Lighthouse 95+ targets and bundle size < 100KB. Run build analysis, identify any issues, and apply fixes.

Purpose: Ensure the site meets all performance requirements before deploy. Fix any issues surfaced by build analysis.
Output: Clean build with all routes under 100KB first load JS, all pages statically rendered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-seo-polish-deploy/06-CONTEXT.md
@.planning/phases/06-seo-polish-deploy/06-RESEARCH.md
@.planning/phases/06-seo-polish-deploy/06-01-SUMMARY.md
@.planning/phases/06-seo-polish-deploy/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build analysis and verify bundle sizes</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current config for optimization opportunities)
    - src/app/[locale]/layout.tsx (verify no unnecessary client-side imports)
    - .planning/phases/06-seo-polish-deploy/06-RESEARCH.md (performance targets)
  </read_first>
  <action>
    1. Run `npx next build` and capture full output. Examine:
       - Route table: every route should show circle icon (static), NOT lambda (dynamic)
       - "First Load JS" column: every route must be under 100KB
       - Any build warnings about large bundles or dynamic rendering

    2. If any route shows lambda (dynamic rendering):
       - Check if the page uses `generateMetadata` with `await params` -- this is fine, Next.js 16 handles this statically with `generateStaticParams`
       - Verify `generateStaticParams` exists for `[locale]` and `[slug]` routes
       - Ensure no `cookies()`, `headers()`, or other dynamic functions are called outside of generateMetadata

    3. If any route exceeds 100KB first load JS:
       - Identify the largest shared chunks in the build output
       - Check for accidentally imported client-side libraries in server components
       - Verify ScrollReveal is the only 'use client' component added in Phase 6
       - Check if navbar/mobile-menu from Phase 2 are reasonably sized
       - Apply tree-shaking: ensure imports are specific (e.g., `import { specific } from 'lib'` not `import * from 'lib'`)

    4. Review `next.config.ts` for production optimization:
       - Ensure `output` is not set to 'standalone' unless needed (default is fine for Vercel)
       - No unnecessary experimental features enabled
       - If using `@next/mdx`, verify it's configured correctly for production

    5. If bundle size is fine (< 100KB per route), document the actual sizes in the SUMMARY for baseline tracking.

    6. Performance checklist for static site:
       - next/font prevents font layout shift (PERF-06 CLS) -- verify fonts are configured with `display: 'swap'`
       - No images in v1 except OG (which is not rendered on pages) -- no image optimization needed
       - Server Components are default -- verify no unnecessary 'use client' directives
       - CSS is minimal (Tailwind purges unused) -- no action needed
  </action>
  <verify>
    <automated>npx next build 2>&1 | grep -E "Route|First Load|○|λ|●" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits with code 0
    - Build output shows circle (○) icon for ALL routes (static rendering)
    - No route shows "First Load JS" exceeding 100KB
    - No build warnings about bundle size
    - Build output is captured and route sizes documented
  </acceptance_criteria>
  <done>Build passes cleanly. All routes are statically rendered with first load JS under 100KB. Performance baseline documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lighthouse audit and visual verification</name>
  <files></files>
  <action>Human runs Lighthouse audit and visually verifies SEO metadata, accessibility features, scroll animations, and active nav link -- all work from Plans 01-03.</action>
  <what-built>Complete SEO metadata, accessibility features, scroll animations, and performance optimization across all pages. Specifically:
    - Every page has meta title, description, OG tags, canonical URL, hreflang alternates
    - JSON-LD Person schema in layout, BreadcrumbList on inner pages, BlogPosting on blog post
    - sitemap.xml and robots.txt generated
    - Skip-to-content link (visible on Tab)
    - Visible focus outlines (2px accent) on all interactive elements
    - Scroll-triggered fade-up animations on sections (except Hero)
    - Active nav link highlighting (accent color)
    - Smooth scroll for anchor links
    - prefers-reduced-motion disables all animations
  </what-built>
  <how-to-verify>
    1. Start dev server: `npx next dev` (or `npx next start` after build)
    2. Open http://localhost:3000 in Chrome

    **SEO verification:**
    - Right-click > View Page Source on homepage: verify `<title>Mattia De Luca — Senior Technical Product Manager | B2B SaaS . Payments</title>`
    - Check for `<meta property="og:title"`, `<meta property="og:description"`, `<link rel="canonical"`, `<link rel="alternate" hreflang`
    - Navigate to /about, verify title is "About — Mattia De Luca"
    - Visit /sitemap.xml -- should list all 14 URLs with alternates
    - Visit /robots.txt -- should show sitemap reference

    **Accessibility verification:**
    - Press Tab on page load -- skip-to-content link should appear top-left with accent background
    - Continue tabbing -- all links and buttons should show 2px accent outline
    - Open Chrome DevTools > Rendering > check "Emulate CSS media feature prefers-reduced-motion: reduce"
    - Scroll through page -- sections should be visible immediately with no animation

    **Animation verification:**
    - Uncheck reduced motion emulation
    - Scroll down slowly on homepage -- each section should fade up smoothly when entering viewport
    - Hero section should NOT animate (always visible)

    **Active nav verification:**
    - Navigate to About page -- "About" link in navbar should be accent colored (#E8A838)
    - Navigate to Work -- "Work" link should be accent colored, About goes back to default

    **Lighthouse audit:**
    - Open Chrome DevTools > Lighthouse
    - Run audit for "Performance", "Accessibility", "Best Practices", "SEO"
    - Target: all scores >= 95

    **Expected results:** All SEO metadata present in page source, skip-to-content works, focus outlines visible, animations play/disabled correctly, active nav highlights work, Lighthouse 95+
  </how-to-verify>
  <verify>Human visual and Lighthouse verification</verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>Lighthouse scores all >= 95. SEO metadata verified in page source. Accessibility features working. Animations play correctly and respect reduced motion.</done>
</task>

</tasks>

<verification>
- Build succeeds with all routes static and under 100KB
- Lighthouse scores all >= 95 (verified by human)
- All SEO metadata present in page source
- Accessibility features working (skip-to-content, focus styles)
- Animations working and respecting reduced-motion
</verification>

<success_criteria>
Build passes with all routes under 100KB first load JS. Lighthouse Performance, Accessibility, Best Practices, and SEO all score 95+. Human verifies visual correctness of animations, focus styles, and SEO metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-polish-deploy/06-03-SUMMARY.md`
</output>
